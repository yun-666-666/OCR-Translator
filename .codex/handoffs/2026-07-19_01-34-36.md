# P12-P15: legacy provider adapters inert + historical docs archived

## Task summary

Made remaining legacy Google/DeepL translation adapters in `handlers/translation_results.py` side-effect free, expanded the live-path inventory contract, and marked historical developer/structure/troubleshooting docs as archived/legacy so they no longer imply those providers are supported.

## Files changed or created

- `handlers/translation_results.py`
- `tests/test_live_path_inventory.py`
- `docs/developer-guide.md`
- `docs/troubleshooting.md`
- `Game_Changing_Translator_Structure.md`
- `.codex/handoffs/2026-07-19_01-34-36.md`

## Backup and rollback

- File backup: `.codex/backups/2026-07-19_01-31-21/`
- Git rollback branch: `codex/backup-before-p12-p15-legacy-provider-and-docs-20260719_013121` at `e98a21d`

## What changed

1. **Legacy adapters inert**
   - `_google_translate`, `_deepl_translate`, `get_deepl_usage`, DeepL log helpers now return archived/no-op results.
   - No `google_api_key_var` / `deepl_api_key_var` reads, no `deepl_api_client` use, no network/request side effects.
   - Custom AI short-log path and utility methods preserved.

2. **Live-path inventory tests expanded**
   - Live product surface modules must exist.
   - Removed inventory includes cache/statistics handlers.
   - AST + runtime probes assert adapters are inert.
   - `app_logic.py` still hard-codes all legacy provider availability flags to `False`.
   - PyInstaller specs continue to exclude legacy provider deps (no re-add).

3. **Docs**
   - Developer guide and structure reference marked archived/legacy with current live-path pointer.
   - Troubleshooting MarianMT/API-key guidance redirected to Custom AI live path.
   - User manuals already had live-path notes; left as product entry points.

## Validation

- `python -m unittest tests.test_live_path_inventory tests.test_custom_ai_startup tests.test_startup_imports -q`: 38 passed.
- `python -m unittest discover -s tests -p 'test_*.py' -q`: 709 passed.
- `python -m py_compile handlers/translation_results.py tests/test_live_path_inventory.py`: passed.
- `git diff --check`: passed.

## Important decisions

- Did not delete adapter method names; kept compatibility stubs so old callers remain harmless.
- Did not reintroduce legacy provider packaging dependencies.
- Did not rewrite entire user manuals; only historical developer/structure/troubleshooting docs that still read as current inventory.

## Remaining notes

- `handlers/translation_context.py` still has DeepL context helpers used only by the now-inert adapters; left alone to avoid unrelated scope expansion.
- Existing untracked cleanup handoffs/docs/release-artifacts were not staged.
