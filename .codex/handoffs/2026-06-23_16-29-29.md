# OCR latency and tesserocr runtime cleanup handoff

## Task summary

Continued the isolated optimization pass without touching UI layout. This round focused on two low-risk behavior improvements:

- Removed an extra per-request copy from streamed Custom AI OCR/translation payload handling while preserving input immutability.
- Added explicit tesserocr API cache clearing on OCR setting changes and translation start/stop boundaries so old `PyTessBaseAPI` instances do not linger across configuration changes.
- Also fixed an OCR worker refresh bug where the runtime language/tessdata refresh timer was effectively never reached during fast loops.

## Files changed

- `custom_ai.py`
- `app_logic.py`
- `worker_threads.py`
- `tests/test_custom_ai.py`
- `tests/test_custom_ai_startup.py`
- `tests/test_latency_optimization.py`

## Backup location

- `.codex/backups/2026-06-23_16-21-39/`
- `.codex/backups/2026-06-23_16-25-20/`
- `.codex/backups/2026-06-23_16-19-17/`

Backed up before edits:

- `custom_ai.py`
- `tests/test_custom_ai.py`
- `app_logic.py`
- `tests/test_custom_ai_startup.py`
- `worker_threads.py`
- `tests/test_latency_optimization.py`

## Important decisions made

- Kept the source project untouched; all edits stayed inside `OCR-Translator-3.9.6-experiment`.
- Preserved Custom AI request immutability while reducing streamed-request overhead by letting the request-prep helper own the single necessary copy.
- Cleared the reusable tesserocr engine cache on OCR parameter/model changes and at translation lifecycle boundaries instead of leaving old API objects around.
- Fixed the OCR worker refresh timer so runtime tessdata/language changes can actually be re-checked after the intended interval.
- Kept UI unchanged.

## Commands/tests run and their results

- Focused red/green checks:
  - `python -m unittest tests.test_custom_ai.CustomAIProviderTests.test_stream_post_preserves_original_payload_while_adding_stream_flag`
  - `python -m unittest tests.test_custom_ai_startup.StartupOptimizationTests.test_tesseract_runtime_cache_is_cleared_when_ocr_parameters_change`
  - `python -m unittest tests.test_latency_optimization.LatencyOcrRuntimeRefreshTests.test_tesseract_runtime_settings_refresh_after_interval_in_fast_loop`
  - Result: each failed before implementation, then passed after implementation.
- Custom AI focused regression:
  - `python -m unittest tests.test_custom_ai.CustomAIProviderTests.test_stream_post_preserves_original_payload_while_adding_stream_flag tests.test_custom_ai.CustomAIProviderTests.test_stream_translation_emits_partial_updates_and_returns_final_text tests.test_custom_ai.CustomAIProviderTests.test_openrouter_profile_adds_latency_provider_routing_without_mutating_input tests.test_custom_ai.CustomAIProviderTests.test_openrouter_profile_preserves_existing_provider_routing tests.test_custom_ai.CustomAIProviderTests.test_none_latency_mode_skips_openrouter_provider_routing`
  - Result: `OK`, 5 tests.
- OCR/runtime focused regression:
  - `python -m unittest tests.test_latency_optimization`
  - Result: `OK`, 18 tests.
- Startup/runtime cleanup regression:
  - `python -m unittest tests.test_custom_ai_startup`
  - Result: included the new tesserocr cache-clear tests, all passing.
- Full tests:
  - `python -m unittest discover -s tests -v`
  - Result: `OK`, 75 tests.
- Root discovery:
  - `python -m unittest discover -v`
  - Result: `OK`, 34 tests.
- Syntax check:
  - `python -m compileall -q app_logic.py custom_ai.py tests\\test_custom_ai_startup.py tests\\test_custom_ai.py`
  - Result: clean.
- Import smoke:
  - `python -c "import app_logic; print('app_logic imported')"`
  - Result: succeeded.

## Current status

The experiment copy is still healthy and verified. The newly added runtime cleanup and stream-path micro-optimization are green, and the OCR worker now refreshes its tessdata/language settings on the intended interval instead of effectively waiting forever.

## Known issues or follow-up work

- I have not yet implemented the follow-up idea from the parallel read-only review to cache tessdata resolution itself. That remains a sensible next low-risk optimization if we want one more pass.
- Historical `backups/` and packaged `dist/` content still contain older Windows OCR mentions; those were intentionally excluded from current-source cleanup.

## Anything the next Codex session should read first

1. This handoff.
2. `app_logic.py`
3. `worker_threads.py`
4. `custom_ai.py`
5. `tests/test_custom_ai_startup.py`
6. `tests/test_custom_ai.py`
7. `tests/test_latency_optimization.py`
