# Project-wide risk-first optimization design handoff

## Task summary

Completed a read-only whole-project audit, compared three optimization
strategies, received approval for the recommended risk-first staged strategy,
and wrote the validated master design for implementation.

No production or test behavior was changed in this phase.

## Files changed or created

- `docs/superpowers/specs/2026-07-13-project-wide-risk-first-optimization-design.md`
- `.codex/handoffs/2026-07-13_02-59-12.md`

## Backup location

`.codex/backups/2026-07-13_03-02-06/`

The design and handoff were initially new files. Before the design self-review
clarified ambiguous CI, packaging, and config-path decisions, both files were
backed up and their SHA-256 hashes matched their source copies.

## Tests and commands run

- `py -m unittest discover -s tests` -> 504 passed.
- `py -m unittest test_custom_ai test_custom_ai_startup -q` -> 35 passed.
- Default `py -m unittest -q` -> only 35 tests, confirming a discovery gap.
- In-memory syntax compilation of 76 tracked Python files -> passed.
- `py -m pip check` -> reported the local-environment conflict
  `torch 2.11.0 requires setuptools<82, but setuptools 82.0.1 is installed`;
  no dependency was changed because this is not yet proven to be a repository
  requirement defect.
- Runtime-log shape aggregation -> identified repeated no-op clear-timeout and
  cooling-profile diagnostics without printing subtitle or credential content.

## Important decisions

- Exclude the untracked `Dango-Translator-Ver6.3.1/` directory from every
  review, edit, stage, commit, and push operation.
- Implement four independently verifiable increments: security boundaries,
  API OCR identity/session isolation, end-to-end deadlines, then engineering
  and release hygiene.
- Preserve the working translation snapshot, adaptive route policy, unified
  cache, metrics, and queue coalescing.
- Do not redesign the UI, make paid API calls, or create a GitHub Release.
- Push the verified final branch only after staged secret and scope checks.

## Remaining issues and next steps

- The written design requires user review under the project brainstorming
  workflow.
- After approval, create detailed implementation plans, back up existing
  targets before editing, implement each increment with TDD, run full
  verification, create the final handoff, commit, and push to GitHub.
