# OCR Translator v3.10.3 release preparation handoff

## Task summary

- Prepared the verified local project state for publication as `v3.10.3`.
- Selected the next free semantic version after the current GitHub Latest release `v3.10.2`.
- Updated package metadata and the changelog to cover the 15 local commits awaiting publication.
- Kept the established desktop interface unchanged while documenting the runtime modularization, Custom AI failover hardening, PaddleOCR cooldown fallback, overlay startup repair, and AI OCR saturation backpressure.

## Files changed or created

- `setup.py`
- `CHANGELOG.md`
- `.codex/handoffs/2026-07-15_23-30-07.md`

Release artifacts are intentionally generated under the ignored `.codex/release-artifacts/` directory after this release commit is created.

## Backup location

- `.codex/backups/2026-07-15_23-28-00/`
- Backed up `setup.py` and `CHANGELOG.md` with workspace-relative paths before editing.

## Tests and commands run

- `py setup.py --version`: reported `3.10.3`.
- `py -m unittest discover -s tests`: 536 tests passed.
- `py -m unittest test_custom_ai test_custom_ai_startup -q`: 35 tests passed.
- `py -B -m py_compile` across setup, application, Custom AI, GUI, handler, overlay, and worker modules: passed.
- `git diff --check`: passed.
- `git diff --cached --check`: passed.
- Tracked sensitive-path scan: no live config, runtime log, `.env`, or release ZIP is tracked.
- High-entropy credential-pattern scan produced one false positive inside the word `risk` in a tracked design filename; a boundary-aware review confirmed it was not a credential.
- `gh auth status`: authenticated as the repository owner with `repo` access.
- `gh release list`: confirmed `v3.10.2` is the current Latest release.
- `gh release view v3.10.3`: confirmed the intended tag/release does not already exist.
- `git ls-remote --tags origin`: confirmed `v3.10.3` is free.

## Important decisions

- Publish the current branch `codex/publish-project-updates-20260706` rather than creating another release branch.
- Use the normal patch release tag `v3.10.3` because this release contains backward-compatible fixes and maintenance improvements.
- Build `OCR-Translator-v3.10.3-source.zip` with `git archive` from the final release commit so ignored configs, logs, backups, and credentials cannot enter the asset.
- Create a real GitHub Release, mark it Latest, and attach the source ZIP with a published SHA-256 digest.
- Do not publish a binary installer because none was rebuilt or verified in this task.

## Remaining publication steps

- Commit the version metadata and this handoff.
- Build the tracked source ZIP and release notes under `.codex/release-artifacts/`.
- Push the current branch.
- Create GitHub Release `v3.10.3` targeting the pushed branch and attach the source ZIP.
- Verify the remote branch, release tag, uploaded asset, and SHA-256 digest all point to the release commit.
