# Task summary

Completed big optimization #26: credential-scoped translation cache identity.

Changing the API key inside an existing profile now changes both the unified
translation cache key and in-flight duplicate key. Results created under the
previous tenant become safe cache misses. Only the provider's non-reversible
credential fingerprint enters request parameters; the SQLite schema stores its
combined parameter hash.

# Files changed or created

- `unified_translation_cache.py`
- `handlers/translation_handler.py`
- `tests/test_custom_ai.py`
- design and plan documents
- this handoff

# Backup location

- `.codex/backups/2026-07-04_19-44-12/`

# Verification

- TDD RED reproduced cache hit and unchanged in-flight key after key rotation.
- Focused credential cache/in-flight tests passed.
- `tests.test_custom_ai`: 159 passed.
- `tests.test_latency_optimization`: 68 passed.
- `discover -s tests`: 267 passed.
- root discovery: 35 passed.
- compileall and diff check passed.

# Decisions

- Reused the existing SHA-256 credential scope helper.
- Kept profile ID isolation in addition to credential isolation.
- Raw API keys never enter cache parameters or persistence.
- `.codex/release-artifacts/` remains untouched.

# Next steps

- Continue with the next independent large optimization after push.
