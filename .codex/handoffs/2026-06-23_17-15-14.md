# API OCR frame cache handoff

## Task summary

Continued the isolated optimization pass in the experiment copy only. This round extended the existing OCR frame cache to the API OCR path, so repeated frames can reuse a cached OCR result instead of re-encoding the screenshot and calling the AI OCR provider again.

## Files changed

- `worker_threads.py`
- `tests/test_latency_optimization.py`

## Backup location

- `.codex/backups/2026-06-23_17-11-41/`

Backed up before edits:

- `worker_threads.py`
- `tests/test_latency_optimization.py`

## Important decisions made

- Kept UI unchanged.
- Kept the source project `C:\Users\wordy\Desktop\OCR-Translator-3.9.6` untouched.
- Kept the OCR scope limited to AI OCR + tesserocr, with no larger OCR engine added.
- Added a small shared screenshot-hash helper so both Tesseract and API OCR paths use the same frame-hash logic.
- Added an API OCR cache-hit branch before WebP conversion and thread-pool submission. If the same frame, provider, language, and region reappear, the cached OCR text is reused immediately.
- Added cache write-back on successful API OCR responses so future repeated frames can hit the cache.
- Kept errors and empty OCR results out of the cache.

## Commands/tests run and their results

- Red test:
  - `python -m unittest tests.test_latency_optimization.LatencyTranslationCacheTests.test_api_ocr_cache_hit_reuses_cached_text_without_webp_or_submit -v`
  - Result: failed as expected before the code change because WebP conversion still happened.
- Focused green tests:
  - `python -m unittest tests.test_latency_optimization.LatencyTranslationCacheTests.test_api_ocr_cache_hit_reuses_cached_text_without_webp_or_submit tests.test_latency_optimization.LatencyTranslationCacheTests.test_successful_api_ocr_response_is_cached_for_frame_reuse -v`
  - Result: `OK`, 2 tests.
- OCR/runtime regression subset:
  - `python -m unittest tests.test_latency_optimization -v`
  - Result: `OK`, 26 tests.
- Startup regression subset:
  - `python -m unittest tests.test_custom_ai_startup -v`
  - Result: `OK`, 13 tests.
- Full test suite:
  - `python -m unittest discover -s tests -v`
  - Result: `OK`, 83 tests.
- Root discovery:
  - `python -m unittest discover -v`
  - Result: `OK`, 34 tests.
- Syntax check:
  - `python -m compileall -q worker_threads.py tests\\test_latency_optimization.py tests\\test_custom_ai_startup.py`
  - Result: clean.
- Real local tesserocr probe:
  - `python -c "import ocr_utils; print(ocr_utils.get_tesseract_languages(None)[0]); print(len(ocr_utils.get_tesseract_languages(None)[1]))"`
  - Result: resolved `C:\Program Files\Tesseract-OCR\tessdata` and reported 161 available languages.
- Source isolation check:
  - `rg -n "get_tesseract_languages|clear_tesseract_languages_cache|test_api_ocr_cache_hit_reuses_cached_text_without_webp_or_submit|test_successful_api_ocr_response_is_cached_for_frame_reuse" "C:\\Users\\wordy\\Desktop\\OCR-Translator-3.9.6" -g "!**/.codex/**" -g "!backups/**" -g "!dist/**"`
  - Result: no matches in the source project.

## Current status

The experiment copy is verified green. API OCR now skips repeated frames through the frame cache, avoiding redundant WebP conversion and AI OCR calls, while successful API OCR results are stored back into the cache for later reuse.

## Known issues or follow-up work

- The cache currently keys by frame hash, provider, source language, preprocessing mode, and region. If a future pass wants stricter cache invalidation, we may want to include any additional OCR knobs that materially affect AI OCR output.
- PyInstaller packaging for tesserocr DLLs/tessdata discovery has not been re-tested in this pass.

## Anything the next Codex session should read first

1. This handoff.
2. `worker_threads.py`
3. `tests/test_latency_optimization.py`
4. `ocr_utils.py`
