# Task summary

Removed the hidden legacy Provider settings/UI compatibility layer while preserving the PaddleOCR and Custom AI live path.

The change removes Google/DeepL/Gemini-special/OpenAI-special/MarianMT widget construction, hide/show lists, old settings persistence, model/browser callbacks, legacy cache-manager startup calls, old UI localization keys, and Custom-AI language-manager compatibility state. The settings cache button now clears the unified translation cache.

Independent review found and this pass fixed four accidental Live Path regressions before commit: Custom Prompt initialization, OCR frame-cache initialization, startup debug-logging state, and a stale `translation_cache.clear()` call.

# Files changed or created

- `app_configuration.py`
- `app_lifecycle.py`
- `app_logic.py`
- `config_manager.py`
- `gui_settings_builder.py`
- `handlers/__init__.py`
- `handlers/cache_manager.py` (deleted)
- `handlers/configuration_handler.py`
- `handlers/translation_results.py`
- `handlers/ui_interaction_handler.py`
- `language_manager.py`
- `ocr_translator_config.example.ini`
- `resources/gui_eng.csv`
- `resources/gui_pol.csv`
- `resources/gui_zh.csv`
- `tests/test_custom_ai_startup.py`
- `tests/test_runtime_logging.py`
- `docs/superpowers/specs/2026-07-17-hidden-legacy-controls-removal-design.md`
- `docs/superpowers/plans/2026-07-17-hidden-legacy-controls-removal.md`
- `.codex/handoffs/2026-07-17_00-40-13.md`

# Backup location

- `.codex/backups/2026-07-17_00-01-15/`
- 25 original files backed up with workspace-relative paths.
- Local rollback branch: `codex/backup-before-legacy-ui-cleanup-20260716`
- Work branch: `codex/remove-hidden-legacy-controls-20260716`

# Tests and commands run

- Baseline: `python -m unittest discover -s tests -q` -> `Ran 612 tests ... OK`
- RED cleanup contract -> failed on `google_api_key_label` as expected.
- RED live language contract -> failed because `get_language_lists` was absent as expected.
- RED cache-manager deletion contract -> failed while `handlers/cache_manager.py` existed as expected.
- RED Live Path preservation contract -> failed while Custom Prompt/OCR frame-cache/debug initialization was missing as expected.
- Final: `python -m unittest discover -s tests -q` -> `Ran 615 tests ... OK`
- Real Tk startup smoke in a temporary config directory -> `startup OK: prompt+ocr-cache+debug+unified-cache verified; legacy controls absent`
- Changed Python files: `python -m py_compile ...` -> passed for 12 files.
- `git diff --check` -> passed.
- Active UI/config legacy-token scan -> no forbidden tokens.
- `handlers/translation_results.py` cache-manager scan -> no references.
- Independent read-only code review and targeted re-review -> no remaining Critical, Important, or Minor findings.

# Important decisions

- This is one bounded cleanup increment, not the full physical deletion of every legacy Provider implementation/resource.
- Kept `paddleocr_source_dir = PaddleOCR-3.7.0` because the backend intentionally uses it as an installed-package fallback path; did not apply the audit document's empty-path proposal.
- Retained secure legacy API-key migration helpers in `config_manager.py`; they no longer create UI controls or write legacy defaults.
- Removed the obsolete cache-manager shim and routed the settings cache action to the unified translation cache.
- Preserved Custom Prompt loading/saving, OCR frame cache, startup debug logging, runtime metrics, overlays, PaddleOCR, and Custom AI profiles.
- No remote push, merge, release, or modification of real user profiles/API keys was performed.

# Remaining issues or next steps

- Dedicated legacy Provider implementation files and several inert methods still exist outside the active UI/runtime route. Remove them in a separate physical-deletion increment with its own dependency/spec/test review.
- `requirements.txt` and PyInstaller specs still need a separate evidence-driven packaging/dependency cleanup.
- Pre-existing untracked handoffs, release notes, `.superpowers/`, and `docs/cleanup/` were intentionally left untouched.
- Implementation commit hash was pending when this handoff was written; use the head of `codex/remove-hidden-legacy-controls-20260716` after the local commit.
