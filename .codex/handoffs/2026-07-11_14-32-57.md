# UI rollback and corrective release handoff

## Task summary

At the user's direction, reverted the unrequested desktop UI work while preserving the translation, performance, and Custom AI profile reliability work already present on the branch. The corrective release target is `v3.10.2`.

## Changes made

- Reverted `40bcb1a` (`test: stabilize Phase 2 screenshot capture`).
- Reverted merge commit `b85dce1` with mainline parent 1. This removes the Amber Signal/Phase 2 interface modernization, associated UI/locale edits, UI screenshots, and visual test while restoring the previous UI resources and Polish documentation.
- Updated `setup.py` to version `3.10.2`.
- Added matching 3.10.2 correction notes to `CHANGELOG.md` and `docs/CHANGELOG.md`.

## Files changed or created

- Restored/deleted UI files through the two revert commits above, including `modern_ui.py`, `gui_builder.py`, UI language resources, Polish documentation, and Phase 2 screenshot/test files.
- Removed the previous UI-integration handoff (`.codex/handoffs/2026-07-11_14-20-44.md`) so current project documentation does not present the reverted UI as active work.
- `setup.py`
- `CHANGELOG.md`
- `docs/CHANGELOG.md`
- `.codex/handoffs/2026-07-11_14-32-57.md`

## Backup

- Current pre-rollback files were copied and SHA-256 verified at `.codex/backups/2026-07-11_14-30-55/` (31 files).
- The removed UI-integration handoff was copied and SHA-256 verified at `.codex/backups/2026-07-11_14-34-12/`.

## Verification

- `py -m unittest discover -s tests -v` — 483 tests passed.
- `py -m unittest discover` — 35 tests passed.
- `git diff --check` — passed for the remaining working-tree release metadata changes.
- `git diff --check HEAD~2..HEAD` reports pre-existing trailing whitespace in restored historical Polish UI files. It was intentionally left untouched so this remains a UI rollback rather than a formatting refactor.

## Important decisions

- No real-provider request was made for this UI-only correction; earlier live-provider diagnosis and translation-pipeline verification remain unchanged.
- Existing untracked local files were not changed or included.
- `v3.10.1` remains an immutable historical release; `v3.10.2` is the corrective release for users who need the established interface.

## Remaining step

- Commit, push the current branch, tag `v3.10.2`, and publish the corrective GitHub release with a source archive.
