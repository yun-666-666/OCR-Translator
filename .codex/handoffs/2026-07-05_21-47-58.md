# Task summary

对 `custom_ai.py` 做了本轮低风险拆分的第一小步：仅搬迁 ASCII-only 常量、枚举值集合和 normalize helper 到新模块，保持 `custom_ai.py` 作为兼容门面继续 re-export 这些公共名称。

# Files changed or created

- `custom_ai.py`
  - 从 `custom_ai_constants.py` 导入并 re-export 已搬迁的常量和 normalize helper。
  - 保留 `CustomAIProvider`、`CustomAIProfileManager`、HTTP transport、payload 构造、response parsing、usage parsing、profile 管理逻辑不动。
  - 保留现有翻译输出 wrapper/preamble 常量在原文件中，避免本轮触碰非 ASCII 文本字面量。
- `custom_ai_constants.py`
  - 新增 Custom AI profile/latency/wire API/structured output/reasoning/output-token 常量。
  - 新增 `normalize_custom_ai_latency_mode`、`normalize_custom_ai_wire_api`、`normalize_custom_ai_structured_output_mode`、`normalize_custom_ai_reasoning_effort`、`normalize_custom_ai_reasoning_request_kind`。
- `.codex/handoffs/2026-07-05_21-47-58.md`
  - 新增本交接文档。

# Backup location

- `.codex/backups/2026-07-05_21-43-22/`

已在修改前备份现有文件：

- `custom_ai.py`

# Tests/commands run and results

- `python -B -m py_compile custom_ai.py custom_ai_constants.py`
  - Passed.
- `python -B -m unittest tests.test_custom_ai`
  - Passed: 209 tests ran in 0.676s.
- `python -B -m unittest tests.test_custom_ai_startup`
  - Passed: 20 tests ran in 0.187s.
- `python -B -m unittest tests.test_latency_optimization`
  - Passed: 103 tests ran in 0.588s.
- `python -B -m unittest discover -s tests -p "test_*.py"`
  - Passed: 365 tests ran in 2.167s.
- `python -B -c "from custom_ai import CustomAIProvider, CustomAIProfileManager, normalize_custom_ai_latency_mode, CUSTOM_AI_LATENCY_MODES; print('PUBLIC_IMPORTS_OK', normalize_custom_ai_latency_mode('adaptive'), 'adaptive' in CUSTOM_AI_LATENCY_MODES)"`
  - Passed: `PUBLIC_IMPORTS_OK adaptive True`.
- `git diff --check -- custom_ai.py custom_ai_constants.py`
  - Passed.
- Workspace secret-pattern scan by filename only:
  - Passed: no `sk-...` pattern matches found in workspace files outside ignored generated/dependency folders.

# Real API verification

Used the relay credential only in memory from the provided attachment file. The credential was not written to code, tests, config, logs, this handoff, or any other persistent file.

- Result: success.
- base_url: `https://shayulajiao.xyz`
- model: `codex-auto-review`
- returned text prefix: `你好`
- elapsed_seconds: `1.234`

# Important decisions

- Kept this as a single small phase. No payload/schema builders, provider methods, HTTP behavior, profile manager code, cache behavior, retry behavior, race/stream/adaptive logic, prompts, or GUI behavior were moved or changed.
- Left `TRANSLATION_OUTPUT_WRAPPER_LABELS` and `TRANSLATION_OUTPUT_PREAMBLES` in `custom_ai.py` for now, even though they are constants, to avoid touching text literal encoding in the first pass.
- Imported every moved public constant/helper back into `custom_ai.py` so existing imports such as `from custom_ai import normalize_custom_ai_latency_mode` remain compatible.

# Remaining issues or next steps

- Other dirty/untracked workspace files existed before this task and were left untouched: `app_logic.py`, `config_manager.py`, `ocr_utils.py`, `worker_threads.py`, prior `.codex/handoffs/`, `.codex/release-artifacts/`, and `tests/test_adaptive_block_size.py`.
- If rollback is needed for this phase, restore `custom_ai.py` from `.codex/backups/2026-07-05_21-43-22/custom_ai.py` and remove the newly created `custom_ai_constants.py`.
- A future small phase could move payload/schema helpers or parsing helpers, but only after a new backup and the same targeted/full/live verification cycle.
