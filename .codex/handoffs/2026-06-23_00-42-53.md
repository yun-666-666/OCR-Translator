# Backups folder size cleanup handoff

## Task summary

Investigated why `OCR-Translator-3.9.6` was about 53 GB and cleaned the
confirmed waste inside `backups/` without creating another backup, per user
instruction.

## Files changed

- Deleted generated/cache/obsolete subtrees under `backups/`:
  - `RUST/`
  - `rust2/`
  - `opus-4-8/`
  - `target/`
  - `build/`
  - `dist/`
  - `artifacts/`
  - `marian_models_cache/`
  - `__pycache__/`
  - common cache folders such as `.pytest_cache`, `.mypy_cache`, `.venv`,
    `.next`, and `node_modules` if present under `backups/`
- Deleted media/log files under `backups/`:
  - `.png`, `.jpg`, `.jpeg`, `.webp`, `.gif`, `.bmp`, `.log`, `.tmp`
- Also deleted root generated/cache directories before the user narrowed the
  final scope to `backups/` only:
  - `build/`
  - root and nested `__pycache__/`

## Backup location

- No new backup was created for this cleanup because the user explicitly said
  `backups/` cleanup did not need a backup.

## Important decisions made

- Stopped further cleanup after the user said the remaining items should stay
  for now.
- Locked the final cleanup boundary to `backups/` after the user clarified
  "只清理backups/里面的东西".
- Did not delete root `dist/`, `artifacts/`, or `marian_models_cache/` after
  inspecting them:
  - `dist/` contains the packaged runnable app.
  - `artifacts/` contains release/source zip artifacts.
  - `marian_models_cache/` is the current model cache.
- Preserved remaining small text/code/config backups under `backups/`.

## Commands/tests run and their results

- Top-level size scan before cleanup:
  - `backups/`: about `52.18 GB`
  - `dist/`: about `813.21 MB`
  - `artifacts/`: about `304.19 MB`
  - `marian_models_cache/`: about `289.66 MB`
  - `build/`: about `189.37 MB`
- `backups/` child scan found three very large old snapshots:
  - `pre_startup_speed_fix_20260601_0001`: about `15.96 GB`
  - `pre_startup_lazy_imports_20260601_0001`: about `15.49 GB`
  - `full_project_backup_20260601_0001`: about `15.49 GB`
- Generated/cache cleanup under `backups/`:
  - Candidate directories: `92`
  - Root delete directories after de-duplication: `62`
  - Deleted directories: `62`
  - Errors: `0`
  - `backups/` reduced from `56,023,954,973` bytes to `207,925,312` bytes.
- Media/log cleanup under `backups/`:
  - Deleted files: `1183`
  - Deleted bytes: `180,740,359`
  - Errors: `0`
- Final verification:
  - `backups/`: `27,184,953` bytes, about `25.93 MB`, `1731` files.
  - Whole project: `1,553,315,464` bytes, about `1.45 GB`.
  - `buildExists=False`
  - `rootPycacheExists=False`

## Current status

The project is no longer large because of `backups/`. The `backups/` folder is
down to about `25.93 MB`; the overall project is about `1.45 GB`.

## Known issues or follow-up work

- Root `build/` and `__pycache__/` were removed before the user clarified the
  final scope as `backups/` only. They are generated/cache directories and can
  be recreated by normal build/test/runtime activity.
- Remaining larger root directories were intentionally left alone:
  - `dist/` about `813.21 MB`
  - `artifacts/` about `304.19 MB`
  - `marian_models_cache/` about `289.66 MB`

## Anything the next Codex session should read first

1. This handoff.
2. `.codex/handoffs/2026-06-23_00-29-15.md` for the preceding Rust cleanup.
3. Re-run a top-level size scan before any additional cleanup.
