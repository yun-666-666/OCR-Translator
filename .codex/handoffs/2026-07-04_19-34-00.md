# Task summary

Completed big optimization #20: semantic race-endpoint deduplication.

Profiles with different IDs but the same normalized URL, model, wire API, and
reasoning effort no longer launch duplicate race requests. The cross-race busy
set uses the same execution identity, preventing a duplicate profile from
bypassing overlap suppression.

# Files changed or created

- `handlers/translation_handler.py`
- `tests/test_custom_ai.py`
- design and plan documents
- this handoff

# Backup location

- `.codex/backups/2026-07-04_19-33-08/`

# Verification

- TDD RED showed duplicate candidate and busy bypass.
- Same-endpoint and busy-endpoint tests passed after implementation.
- `tests.test_custom_ai`: 148 passed.
- `tests.test_latency_optimization`: 68 passed.
- `discover -s tests`: 256 passed.
- root discovery: 35 passed.
- diff check passed.

# Decisions

- Profile IDs remain part of cache ownership but not network execution identity.
- Distinct endpoints with the same model remain valid race candidates.
- `.codex/release-artifacts/` remains untouched.

# Next steps

- Continue with the next independent large optimization after push.
