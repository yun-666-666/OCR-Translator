# C01/C02: Config location migration + atomic save

## Task summary

Application config now resolves to a stable app-data directory (with env/test overrides), migrates once from legacy CWD config without overwriting an existing target, and saves via same-directory temp + fsync + `os.replace`.

## Backup / rollback

- Git branch: `codex/backup-before-c01-c02-config-location-atomic-save-2026-07-19_01-25-55` @ `3128147`
- File backup: `.codex/backups/2026-07-19_01-25-55/`

## Changes

### `config_manager.py`
- Path resolver: `OCR_TRANSLATOR_CONFIG_DIR` override, stable app-data default, isolated test dir
- `migrate_legacy_config_if_needed()` one-shot copy; never deletes legacy; never overwrites target
- Atomic save helpers with module-local stale temp cleanup only
- `load_app_config` / `save_app_config` use resolved path + atomic write

### Tests
- New `tests/test_config_manager.py` for path/migration/atomic failure cases
- Existing CWD-based config tests switched to env override isolation

## Verification

- config/startup/layout/provider config tests → OK
- `python -m unittest discover -s tests` → 705 OK
- `py_compile` / `git diff --check` → clean
