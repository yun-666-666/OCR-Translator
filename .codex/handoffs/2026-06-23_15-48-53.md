# Tesserocr OCR migration handoff

## Task summary

Optimized the isolated experiment copy by removing the old Windows OCR fallback path and migrating the local Tesseract path to a reusable `tesserocr` API wrapper. UI layout was left unchanged.

## Files changed

- `ocr_utils.py`
- `worker_threads.py`
- `app_logic.py`
- `handlers/configuration_handler.py`
- `handlers/ui_interaction_handler.py`
- `requirements.txt`
- `setup.py`
- `GameChangingTranslator.spec`
- `GameChangingTranslator_GPU.spec`
- `docs/installation.html`
- `docs/installation_pl.html`
- `LICENSE`
- `tests/test_latency_optimization.py`
- `tests/test_custom_ai_startup.py`
- `tests/test_startup_imports.py`
- `test_custom_ai_startup.py`

## Backup location

- `.codex/backups/2026-06-23_15-25-03/`

Backed up before edits:

- `ocr_utils.py`
- `worker_threads.py`
- `app_logic.py`
- `handlers/configuration_handler.py`
- `handlers/ui_interaction_handler.py`
- `requirements.txt`
- `setup.py`
- `tests/test_latency_optimization.py`
- `tests/test_custom_ai_startup.py`
- `tests/test_startup_imports.py`
- `test_custom_ai_startup.py`
- `GameChangingTranslator.spec`
- `GameChangingTranslator_GPU.spec`
- `docs/installation.html`
- `docs/installation_pl.html`
- `LICENSE`

## Important decisions made

- Kept the source project untouched; all changes stayed in `OCR-Translator-3.9.6-experiment`.
- Removed Windows OCR entirely rather than hiding it.
- Replaced the Tesseract subprocess wrapper with a reusable `tesserocr` engine cache.
- Preserved the existing `tesseract_path` UI entry and interpreted it as the source for the nearby `tessdata` directory.
- Kept `mss` capture, Custom AI OCR, and the existing UI structure.
- Updated packaging/docs/license references so the repo now points at `tesserocr` instead of `pytesseract`.

## Commands/tests run and their results

- Focused regression:
  - `python -m unittest tests.test_latency_optimization.LatencyTesserocrTests -v`
  - Result: `OK`
- Latency suite:
  - `python -m unittest discover -s tests -p 'test_latency_optimization.py' -v`
  - Result: `OK`, 13 tests
- Full test suite:
  - `python -m unittest discover -s tests -v`
  - Result: `OK`, 67 tests
- Root discovery:
  - `python -m unittest discover -v`
  - Result: `OK`, 34 tests
- Startup smoke:
  - `python -c "import json, sys; import main; ..."`
  - Result: `[]`
- Syntax check:
  - `python -m py_compile worker_threads.py ocr_utils.py app_logic.py handlers\\configuration_handler.py handlers\\ui_interaction_handler.py tests\\test_latency_optimization.py tests\\test_custom_ai_startup.py tests\\test_startup_imports.py test_custom_ai_startup.py`
  - Result: clean
- Dependency install attempt:
  - `python -m pip install "tesserocr>=2.10.0"`
  - Result: failed because the local environment has Tesseract installed, but pip could not build `tesserocr` without the needed native library/import-lib setup.
- Dependency install retry with Tesseract paths exposed:
  - `$env:PATH = 'C:\\Program Files\\Tesseract-OCR;' + $env:PATH; $env:LIBPATH = 'C:\\Program Files\\Tesseract-OCR'; python -m pip install "tesserocr>=2.10.0"`
  - Result: failed again; `tesseract.exe` was found, but the install directory has DLLs and no `.lib` import libraries for the `tesserocr` source build.
- Official Windows wheel install:
  - `python -m pip install "https://github.com/simonflueckiger/tesserocr-windows_build/releases/download/tesserocr-v2.10.0-tesseract-5.5.2/tesserocr-2.10.0-cp312-cp312-win_amd64.whl"`
  - Result: installed `tesserocr-2.10.0` successfully.
- Live tesserocr OCR smoke:
  - Generated a small PIL image with `HELLO OCR`, initialized the reusable engine with `C:\\Program Files\\Tesseract-OCR\\tessdata`, and called `engine.recognize(...)`.
  - Result: returned `'HELLOOCR'`, confirming the real API path initializes and recognizes text.
- Full test suite after installing the real wheel:
  - `python -m unittest discover -s tests -v`
  - Result: `OK`, 67 tests

## Current status

Code and tests are green in the experiment copy. The repository no longer exposes Windows OCR in production paths, and the local Tesseract flow now targets and successfully imports/runs `tesserocr` in the current Python 3.12 environment.

## Known issues or follow-up work

- `pip install tesserocr>=2.10.0` still tries to build from source in this environment and fails because the installed Tesseract folder has DLLs but no `.lib` import libraries.
- The working install path for this environment is the official Windows CPython 3.12 wheel from `simonflueckiger/tesserocr-windows_build`.
- Several old `pytesseract` mentions remain only in tests/assertions that guard against regressions.

## Anything the next Codex session should read first

1. This handoff.
2. `ocr_utils.py`
3. `worker_threads.py`
4. `tests/test_latency_optimization.py`
