# OCR stability flush uses delay-aware guarded UI scheduler

## Candidate selection

Reviewed remaining live-path Tk / `root.after` sites after Round 4 (`37ff2f3`).

Candidates considered:

1. OCR stability flush still used raw delayed `app.root.after(delay_ms, ...)` without shutdown / `winfo_exists` / TclError guards. **Selected** now that `_schedule_ui_callback` supports `delay_ms`.
2. Instant-cache Tk var read inside `start_async_translation`. Deferred: entrypoints already UI-scheduled.
3. Translation-sequence counter TOCTOU. Deferred: medium risk, broader locking design.
4. Compatibility `run_api_ocr` live Tk fallbacks without snapshot. Deferred: production path always supplies snapshot.
5. Worker latency-mode fallback Tk reads when snapshot missing. Deferred: production freezes latency mode in request snapshot.

Selection criteria met for item 1:

- High confidence static proof: same unguarded delayed schedule class as pending flush.
- Low risk: reuse already-landed delay-aware helper; preserve generation / flush flag rollback; leave pending OCR text intact.
- <=4 files: only `worker_ocr.py` + tests + handoff.
- Deterministic tests without network.
- Stability benefit: avoids post-shutdown OCR stability timer scheduling.

## Task summary

`_schedule_ocr_stability_flush` now schedules `_flush_ocr_stability_candidate` through `_schedule_ui_callback(..., delay_ms=...)`. Schedule failure clears `flush_scheduled` / deadline for the current generation and returns without raising, while keeping the pending OCR candidate for a later reschedule.

## Files changed or created

- `worker_ocr.py`
- `tests/test_latency_optimization.py`
- `.codex/handoffs/2026-07-20_22-59-23.md`

## Backup and rollback

- Backup: `.codex/backups/2026-07-20_22-59-23/`
- Branch: `codex/grok-auto-optimization-20260720`
- Rollback branch: `codex/backup-before-grok-auto-optimization-20260720-222943`

## Validation

- `python -m unittest tests.test_latency_optimization.LatencyOcrStabilityGateTests -v`: 14 passed.
- `python -m unittest tests.test_latency_optimization -q`: 182 passed.
- `python -m unittest discover -s tests -p 'test_*.py' -q`: 727 passed.
- `python -m py_compile worker_ocr.py tests/test_latency_optimization.py`: passed.
- `git diff --check -- worker_ocr.py tests/test_latency_optimization.py`: passed.

## Re-review of this diff

- Happy-path delayed flush still works via existing route tests.
- Closing-app schedule failure leaves pending text and clears flush flags.
- Local OCR UI submit path remains on `_schedule_ui_callback` delay 0.
- No keep_linebreaks / API OCR / profile-refresh regressions introduced.

## Remaining issues or next steps

- Remaining candidates are medium-risk or lack a clean production-path correctness win under the current bar.
- Translation-sequence locking remains a broader follow-up.
