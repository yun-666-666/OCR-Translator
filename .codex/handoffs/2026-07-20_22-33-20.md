# Translation thread uses published OCR snapshot for inactivity clear

## Candidate selection

Reviewed live-path workers after the verified checkpoint (`b95ef24`).

Candidates considered:

1. Translation thread still called `app.get_ocr_model_setting()` / `is_api_based_ocr_model` for inactivity clear gating (`worker_threads.py` translation loop). **Selected.**
2. Translation-sequence counter TOCTOU across threads. Deferred: medium risk, broader locking design.
3. Pending translation flush / profile refresh use raw `root.after` rather than `_schedule_ui_callback`. Deferred: needs careful generation/deadline interaction review; not the highest correctness win this round.
4. Instant-cache display path still reaches Tk vars only after UI scheduling (already fixed entrypoints). No remaining high-confidence worker entry.
5. Cosmetic refactor of facade helpers. Rejected: no user-visible correctness gain.

Selection criteria met for item 1:

- High confidence static proof: `get_ocr_model_setting` reads `ocr_model_var.get()` on a worker thread.
- Low risk: replace with already-published `CaptureUISnapshot.is_api_based`.
- <=4 files: only `worker_threads.py` + tests.
- Deterministic tests without network.
- User-visible correctness/stability: avoids Tk access from translation worker; prevents API/local clear-gate misclassification from live UI races.

## Task summary

`run_translation_thread` no longer live-reads the OCR model Tk variable when deciding whether local inactivity clear is allowed. It reads the published capture UI snapshot with `allow_unpublished_build=False` and uses `snapshot.is_api_based`. Missing snapshots skip the clear gate instead of falling back to Tk.

## Files changed or created

- `worker_threads.py`
- `tests/test_latency_optimization.py`
- `.codex/handoffs/2026-07-20_22-33-20.md`

## Backup and rollback

- Backup: `.codex/backups/2026-07-20_22-31-36/`
- Branch: `codex/grok-auto-optimization-20260720`
- Rollback branch: `codex/backup-before-grok-auto-optimization-20260720-222943`

## Validation

- `python -m unittest tests.test_latency_optimization.TranslationInactivityClearTests -v`: 8 passed.
- `python -m unittest tests.test_latency_optimization -q`: 177 passed.
- `python -m unittest discover -s tests -p 'test_*.py' -q`: 722 passed.
- `python -m py_compile worker_threads.py tests/test_latency_optimization.py`: passed.
- `git diff --check -- worker_threads.py tests/test_latency_optimization.py`: passed.

## Re-review of this diff

- Original acceptance covered: local snapshot schedules clear; API snapshot does not; unpublished snapshot does not fall back to Tk.
- No cache/snapshot/keep_linebreaks/UI-routing baseline regressions introduced.
- Scope stayed inside the translation-thread clear gate only.
- Baseline commit fixes remain intact.

## Remaining issues or next steps

- Translation-sequence locking remains a broader follow-up.
- Raw `root.after` sites in pending flush / profile refresh may still warrant `_schedule_ui_callback` alignment later after separate risk review.
