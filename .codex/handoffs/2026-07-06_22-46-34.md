# Task summary

Prepared the current branch for release after the Tesseract-removal work and updated the main introduction surfaces to match the current project shape.

This turn changed the public project introduction in `README.md` and `index.html` so the first-run description now emphasizes:

- PaddleOCR as the supported local OCR backend.
- Custom AI profiles as the supported OpenAI-compatible translation path.
- Optional Custom AI OCR for image-based providers.
- The screen-capture -> OCR -> translation -> cache -> overlay runtime flow.
- The removal of legacy local OCR runtime support without reintroducing old backend tokens.

The release commit is intended to include the broad project changes described in `.codex/handoffs/2026-07-06_22-26-57.md`, plus this documentation refresh and handoff.

# Files changed or created

Changed in this turn:

- `README.md`
- `index.html`

Created in this turn:

- `.codex/handoffs/2026-07-06_22-46-34.md`

Release commit also includes the previously completed runtime/config/UI/test/docs cleanup from:

- `.codex/handoffs/2026-07-06_22-26-57.md`

# Backup location

Backups created before this turn's existing-file edits:

- `.codex/backups/2026-07-06_22-41-18/`
  - `README.md`
  - `index.html`

Earlier backups used by the release changes:

- `.codex/backups/2026-07-06_21-36-34/`
- `.codex/backups/2026-07-06_22-22-38/custom_prompt.txt`

# Tests/commands run and results

- `py -B -m py_compile app_logic.py config_manager.py worker_threads.py ocr_utils.py gui_builder.py handlers\configuration_handler.py handlers\display_manager.py handlers\ui_interaction_handler.py language_manager.py main.py translation_utils.py tests\test_latency_optimization.py tests\test_paddle_ocr_backend.py tests\test_custom_ai_startup.py tests\test_custom_ai.py test_custom_ai_startup.py tests\test_startup_imports.py`
  - Passed.
- `py -m unittest tests.test_paddle_ocr_backend tests.test_custom_ai_startup tests.test_startup_imports tests.test_latency_optimization tests.test_custom_ai test_custom_ai_startup`
  - Passed: 367 tests.
- `py -m unittest tests.test_overlay_startup tests.test_overlay_geometry`
  - Passed: 10 tests.
- `git diff --check`
  - Passed.
- `rg -n "tesseract|Tesseract|pytesseract|confidence_threshold|image_preprocessing_mode|adaptive_block_size|adaptive_c|remove_trailing_garbage" --glob "!.git/**" --glob "!node_modules/**" --glob "!dist/**" --glob "!build/**" --glob "!.next/**" --glob "!.venv/**" --glob "!__pycache__/**" --glob "!.codex/backups/**" --glob "!*.log*"`
  - Passed: no matches.
- `rg -n "tesserocr|Tesserocr|pytesseract|tessdata|TESSDATA" --glob "!.git/**" --glob "!node_modules/**" --glob "!dist/**" --glob "!build/**" --glob "!.next/**" --glob "!.venv/**" --glob "!__pycache__/**" --glob "!.codex/backups/**" --glob "!*.log*"`
  - Passed: no matches.

# Important decisions

- Treated `README.md` and `index.html` as the main introduction documents for this release.
- Replaced the old homepage marketing copy with a calmer current-runtime overview.
- Updated repository links in the intro page to the current fork remote.
- Avoided old backend literals in new introduction copy so the release still satisfies the previous residue-scan checks.
- Left runtime code untouched in this turn; this was documentation and release preparation only.

# Remaining issues or next steps

- No known functional blockers remain.
- The branch still needs to be committed and pushed after this handoff is created.
