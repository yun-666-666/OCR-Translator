# Task summary

Reviewed `docs/cleanup/project-review-optimization-2026-07-17.md`, identified it as the newly created Grok project review, and implemented one evidence-backed cleanup increment.

Removed the closed Gemini/OpenAI dedicated-provider and MarianMT source clusters, deleted provider-only CSV inventories, removed dead Marian facade methods, and aligned the CPU/GPU PyInstaller specs and build launcher with the PaddleOCR + Custom AI live path.

Added a repository contract test so the deleted inventory, retired packaging dependencies, and destructive PyTorch install/uninstall build behavior cannot silently return.

# Files changed or created

Modified:

- `GameChangingTranslator.spec`
- `GameChangingTranslator_GPU.spec`
- `compile_app.py`
- `handlers/translation_results.py`
- `tests/test_runtime_logging.py`

Created:

- `tests/test_live_path_inventory.py`
- `docs/superpowers/specs/2026-07-17-live-path-inventory-cleanup-design.md`
- `docs/superpowers/plans/2026-07-17-live-path-inventory-cleanup.md`
- `.codex/handoffs/2026-07-17_14-33-27.md`

Deleted source modules:

- `marian_mt_translator.py`
- `convert_marian.py`
- `handlers/gemini_provider.py`
- `handlers/gemini_ocr_provider.py`
- `handlers/gemini_models_manager.py`
- `handlers/openai_provider.py`
- `handlers/openai_ocr_provider.py`
- `handlers/openai_models_manager.py`
- `handlers/ocr_provider_base.py`
- `handlers/llm_provider_base.py`

Deleted provider-only resources:

- `resources/deepl_trans_source.csv`
- `resources/deepl_trans_target.csv`
- `resources/gemini_models.csv`
- `resources/gemini_trans_source.csv`
- `resources/gemini_trans_target.csv`
- `resources/openai_models.csv`
- `resources/openai_trans_source.csv`
- `resources/openai_trans_target.csv`
- `resources/MarianMT_models_short_list.csv`
- `resources/MarianMT_select_models.csv`

# Backup location

- `.codex/backups/2026-07-17_14-11-38/`
- 25 pre-existing files were backed up with workspace-relative paths before modification or deletion.
- New test/design/plan/handoff files did not exist and therefore required no pre-edit backup.

# Tests and commands run

- Baseline: `python -m unittest discover -s tests -q` -> `Ran 615 tests ... OK`.
- Initial inventory RED: `python -m unittest tests.test_live_path_inventory -v` -> failed on all 10 legacy source files, all 10 provider resources, and both stale specs as expected.
- Build-launcher RED: the new no-package-mutation/Paddle validation contracts failed against the old PyTorch install/uninstall launcher as expected.
- Focused startup/logging: `python -m unittest tests.test_runtime_logging tests.test_custom_ai_startup -v` -> `Ran 47 tests ... OK`.
- Focused inventory/build contracts before final verifier fix: `python -m unittest tests.test_live_path_inventory -v` -> `Ran 6 tests ... OK`.
- Final focused inventory/build contracts: `python -m unittest tests.test_live_path_inventory -v` -> `Ran 7 tests ... OK`.
- Final full suite: `python -m unittest discover -s tests -q` -> `Ran 622 tests ... OK`.
- Python/spec compile: `python -m py_compile compile_app.py handlers/translation_results.py tests/test_runtime_logging.py tests/test_live_path_inventory.py GameChangingTranslator.spec GameChangingTranslator_GPU.spec` -> passed.
- Final CPU PyInstaller build: `python -m PyInstaller --noconfirm --workpath <temporary> --distpath <temporary> GameChangingTranslator.spec` -> exit 0.
- Final package inventory: 626.5 MiB / 2025 files; no packaged `torch`, `transformers`, `sentencepiece`, Gemini SDK, or DeepL artifacts.
- Packaged executable smoke: hidden launch for 10 seconds -> process remained running; logs showed config migration, live language/UI resources, and application initialization; the smoke process was then stopped.
- Temporary build-validation output: removed two exact `.codex/build-validation/` directories created by this task, reclaiming about 0.77 GiB. The generated output is reproducible from the spec.
- Paddle verifier root-cause reproduction: direct `paddleocr` import failed because installed `modelscope` detected a broken optional Torch DLL (`WinError 127`). Masking optional Torch discovery made `paddleocr 3.7.0` and `paddle 3.3.1` import normally.
- Verifier regression RED/GREEN: `test_paddle_verifier_ignores_optional_torch_discovery` failed before the guard and passed after it.
- Live verifier: `compile_app.verify_paddle_installation(require_cuda=False)` -> Paddle 3.3.1, PaddleOCR 3.7.0, CUDA false, exit 0.
- `git diff --check` -> passed.
- Active source/spec scans -> no references to deleted module names outside intentional inventory/startup assertions.

# Important decisions

- Kept `resources/google_trans_source.csv` and `resources/google_trans_target.csv`. Despite Grok recommending deletion with other provider CSVs, `LanguageManager` currently uses them as the supported Custom AI source/target language inventory.
- Kept secure legacy-key stripping/migration in `config_manager.py`; it protects old user configs and does not revive provider UI/runtime paths.
- Removed explicit Marian/PyTorch/Transformers packaging, but did not assume PaddleX has no transitive optional integrations. The specs exclude optional Torch/Transformers/SentencePiece artifacts while retaining PaddleOCR/PaddleX's real non-Torch dependencies.
- Replaced `compile_app.py` package mutation with environment validation. CPU builds require importable Paddle/PaddleOCR; GPU builds additionally require a CUDA-enabled Paddle distribution.
- Limited packaged documentation to `docs/user-manual.html` and `docs/CHANGELOG.md` rather than shipping the whole `docs/` tree with cleanup audits and agent plans.
- Did not edit generated `MANIFEST.txt`; it remains a stale generated inventory and should be regenerated or removed in a dedicated packaging/release task.
- Did not implement Grok's diagnostics UI, new subtitle mode, cache changes, or large-module/test splits because the review supplied no current runtime measurements proving those changes are the next bottleneck.

# Remaining issues or next steps

- Run and smoke-test `GameChangingTranslator_GPU.spec` on a machine with `paddlepaddle-gpu`; the current machine has a CPU Paddle build, so the new build launcher intentionally rejects the GPU route.
- Decide in a separate documentation increment whether to rewrite/archive `docs/developer-guide.md` and `Game_Changing_Translator_Structure.md`; they still describe removed providers and resources.
- Regenerate or retire `MANIFEST.txt` in a packaging/release-specific task rather than hand-editing generated output.
- Remaining Gemini-era names in active Custom AI OCR concurrency fields and the still-present DeepL compatibility helpers should be handled as separate behavior-locked cleanup increments.
- No push, merge, release, or user configuration/profile modification was performed.
