# Tessdata resolver caching handoff

## Task summary

Continued the isolated optimization pass without touching UI layout. This round cached tessdata resolution, kept the cache explicitly clearable, and wired cache clearing into OCR lifecycle boundaries so path/environment changes do not keep using stale lookup results.

## Files changed

- `ocr_utils.py`
- `app_logic.py`
- `tests/test_latency_optimization.py`
- `tests/test_custom_ai_startup.py`

## Backup location

- `.codex/backups/2026-06-23_16-32-13/`

Backed up before edits:

- `ocr_utils.py`
- `app_logic.py`
- `tests/test_latency_optimization.py`
- `tests/test_custom_ai_startup.py`

## Important decisions made

- Kept the source project untouched; all changes stayed inside `OCR-Translator-3.9.6-experiment`.
- Added an `lru_cache` around tessdata resolution, keyed by the configured Tesseract path plus `TESSDATA_PREFIX` and `LOCALAPPDATA`.
- Added `clear_tessdata_dir_cache()` so runtime settings changes can force fresh filesystem probing when needed.
- Wired OCR runtime cache clearing into OCR parameter changes, OCR model changes, and translation start/stop boundaries.
- Left UI unchanged.

## Commands/tests run and their results

- Red tests before implementation:
  - `python -m unittest tests.test_latency_optimization.LatencyTesserocrTests.test_tesseract_path_resolution_is_cached_until_cleared tests.test_latency_optimization.LatencyTesserocrTests.test_tesseract_path_resolution_cache_key_includes_tessdata_prefix`
  - Result: failed as expected because `clear_tessdata_dir_cache` did not exist yet.
- Focused regression after implementation:
  - `python -m unittest tests.test_latency_optimization.LatencyTesserocrTests tests.test_custom_ai_startup.StartupOptimizationTests.test_tesseract_runtime_cache_is_cleared_when_ocr_parameters_change tests.test_custom_ai_startup.StartupOptimizationTests.test_tesseract_runtime_cache_is_cleared_when_ocr_model_changes tests.test_custom_ai_startup.StartupOptimizationTests.test_tesseract_runtime_cache_clear_is_wired_into_start_and_stop_paths`
  - Result: `OK`, 11 tests.
- OCR/startup focused regression:
  - `python -m unittest tests.test_latency_optimization tests.test_custom_ai_startup`
  - Result: `OK`, 33 tests.
- Full test suite:
  - `python -m unittest discover -s tests -v`
  - Result: `OK`, 77 tests.
- Root discovery:
  - `python -m unittest discover -v`
  - Result: `OK`, 34 tests.
- Syntax check:
  - `python -m compileall -q ocr_utils.py app_logic.py tests\\test_latency_optimization.py tests\\test_custom_ai_startup.py`
  - Result: clean.
- Source isolation check:
  - `rg -n "clear_tessdata_dir_cache|_resolve_tessdata_dir_from_tesseract_path_cached|test_tesseract_path_resolution_is_cached_until_cleared|test_tesseract_path_resolution_cache_key_includes_tessdata_prefix" "C:\\Users\\wordy\\Desktop\\OCR-Translator-3.9.6" -g "!**/.codex/**" -g "!backups/**" -g "!dist/**"`
  - Result: no matches in the source project.

## Current status

The experiment copy is verified. Tessdata resolution is now cached and explicitly clearable, OCR runtime changes clear both the tessdata resolver cache and the reusable tesserocr engine cache, and the current test suite is green.

## Known issues or follow-up work

- A further low-risk optimization could still be to reduce repeated tessdata probing even more aggressively if we decide to cache the “current path” at a higher level, but the current lru-cache pass already removed the repeated probes.
- Historical `backups/` and packaged `dist/` content still contain older Windows OCR mentions; those remain intentionally excluded from current-source cleanup.

## Anything the next Codex session should read first

1. This handoff.
2. `ocr_utils.py`
3. `app_logic.py`
4. `tests/test_latency_optimization.py`
5. `tests/test_custom_ai_startup.py`
