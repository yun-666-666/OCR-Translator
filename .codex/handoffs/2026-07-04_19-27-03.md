# Task summary

Completed big optimization #17: immutable race-request cache identity.

Race-mode translation now captures custom prompt, line-break mode, and approved
context before dispatch. Candidate calls, the winning-profile cache entry, and
the active-profile alias all use that exact snapshot. UI changes while HTTP
calls are active can no longer store an old-result translation under a new
configuration's cache key.

# Files changed or created

- `handlers/translation_handler.py`
- `tests/test_custom_ai.py`
- `docs/superpowers/specs/2026-07-04-race-cache-request-snapshot-design.md`
- `docs/superpowers/plans/2026-07-04-race-cache-request-snapshot.md`
- `.codex/handoffs/2026-07-04_19-27-03.md`

# Backup location

- `.codex/backups/2026-07-04_19-26-07/`

# Tests and commands run

- Focused RED reproduced the missing request-snapshot interface.
- Focused race snapshot and healthy-alternative tests passed.
- `python -m unittest tests.test_custom_ai -q`: 143 passed.
- `python -m unittest tests.test_latency_optimization -q`: 68 passed.
- `python -m unittest discover -s tests -q`: 251 passed.
- `python -m unittest discover -q`: 35 passed.
- `python -m compileall -q handlers/translation_handler.py tests`: passed.
- `git diff --check`: passed.

# Important decisions

- Snapshot only output-affecting request semantics.
- Reused one cache-parameter builder with explicit overrides.
- Kept existing callers compatible by reading UI state when overrides are not
  provided.
- Applied the same snapshot to winner and active-profile alias cache entries.
- Left `.codex/release-artifacts/` untouched.

# Remaining issues or next steps

- Continue with the next independent large optimization after push.
