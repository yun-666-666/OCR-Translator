# Custom AI profile form metadata preservation handoff

## Task summary

Fixed the optimized experiment copy only. The screenshot error happened because the Custom AI profile form rebuilt a temporary profile from visible fields only, dropping hidden metadata such as `wire_api = responses` and `reasoning_effort = xhigh`. When the user edited the form name/URL and clicked Fetch Models, the temporary profile fell back to the default Chat Completions mode, so model fetching tried `/v1/models` and `/models` and surfaced the model-list error instead of using the configured Responses profile fallback.

## Files changed

- `gui_builder.py`
- `tests/test_custom_ai.py`

## Backup location

Backup created before this round of edits:

- `.codex/backups/2026-06-23_20-33-42/`
  - `gui_builder.py`
  - `tests/test_custom_ai.py`

An earlier backup from the interrupted handoff also exists:

- `.codex/backups/2026-06-23_20-30-25/`
  - `gui_builder.py`
  - `tests/test_custom_ai.py`

## Important decisions made

- Kept the source project `C:\Users\wordy\Desktop\OCR-Translator-3.9.6` out of scope.
- Kept the fix narrow: the form now preserves metadata from the selected profile when building values for save, test, and fetch-model actions.
- Preferred the selected profile ID over name lookup, because the screenshot shows the name field edited to `222` while the selected profile was still the existing Responses profile.
- Preserved `wire_api` and `reasoning_effort` only when an existing selected/name-matched profile is found. New profiles still rely on the profile manager defaults.
- Caught an `apply_patch` path issue during the test edit: a test-only patch briefly landed in the source project. Restored `C:\Users\wordy\Desktop\OCR-Translator-3.9.6\tests\test_custom_ai.py` from `.codex/backups/2026-06-23_20-00-53/tests/test_custom_ai.py`, then verified the source project no longer contains the new helper/test markers.

## Commands/tests run and their results

- Red focused regression before implementation:
  - `python -m unittest tests.test_custom_ai.ProfileFormValuesTests.test_profile_form_values_preserve_selected_responses_metadata_when_name_is_edited -v`
  - Result: failed with `AttributeError: module 'gui_builder' has no attribute 'build_custom_ai_profile_values_from_form'`.
- Green focused regression after implementation:
  - `python -m unittest tests.test_custom_ai.ProfileFormValuesTests.test_profile_form_values_preserve_selected_responses_metadata_when_name_is_edited -v`
  - Result: `OK`.
- Custom AI suite:
  - `python -m unittest tests.test_custom_ai -v`
  - Result: `Ran 42 tests`, `OK`.
- Syntax check:
  - `python -m compileall -q gui_builder.py tests\test_custom_ai.py`
  - Result: clean.
- Full tests discovery:
  - `python -m unittest discover -s tests -v`
  - Result: `Ran 108 tests`, `OK`.
- Root discovery:
  - `python -m unittest discover -v`
  - Result: `Ran 34 tests`, `OK`.
- Screenshot-style Responses model-list simulation:
  - PowerShell here-string piped into `python -`
  - Result: returned `['gpt-5.4']` for `https://ebbandflow.online/v1` with `wire_api = responses`; attempted URLs were `https://ebbandflow.online/v1/models` and `https://ebbandflow.online/models`, then fell back to the configured model instead of raising the dialog error.
- Source isolation checks:
  - `rg -n "build_custom_ai_profile_values_from_form|class ProfileFormValuesTests|_find_selected_custom_ai_profile|_read_var" C:\Users\wordy\Desktop\OCR-Translator-3.9.6\gui_builder.py C:\Users\wordy\Desktop\OCR-Translator-3.9.6\tests\test_custom_ai.py`
  - Result: no matches.
  - `Compare-Object` between source `tests/test_custom_ai.py` and its `.codex/backups/2026-06-23_20-00-53` copy returned no differences.

## Current status

The optimized experiment copy keeps Responses profile metadata through the profile form path. This should stop Fetch Models, Test, Save, translation, and AI OCR form actions from accidentally downgrading the selected profile to Chat Completions when the UI fields are edited.

## Known issues or follow-up work

- No packaged executable or `dist` output was rebuilt in this pass.
- No live external API call was made; the model-list behavior was verified with a deterministic local fake client and the existing unit tests.
- The UI still does not visibly expose `wire_api`; this fix only preserves existing hidden metadata.

## Anything the next Codex session should read first

1. This handoff.
2. `gui_builder.py`, especially `build_custom_ai_profile_values_from_form`.
3. `tests/test_custom_ai.py`, especially `ProfileFormValuesTests`.
4. `.codex/handoffs/2026-06-23_19-39-32.md` for the earlier Responses API support work.
