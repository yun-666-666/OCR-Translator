# Custom AI OCR source-language routing handoff

## Task summary

Continued the isolated optimization pass in the experiment copy only. This round tightened Custom AI OCR language routing so OCR requests use the Custom AI OCR source-language setting and add a lightweight source-language hint to the OCR prompt when the setting is not `auto`.

## Files changed

- `custom_ai.py`
- `worker_threads.py`
- `tests/test_custom_ai.py`
- `tests/test_latency_optimization.py`

## Backup location

Backups created before this edit group:

- `.codex/backups/2026-06-23_18-02-51/`
  - `custom_ai.py`
  - `worker_threads.py`
  - `tests/test_custom_ai.py`
  - `tests/test_latency_optimization.py`

## Important decisions made

- Kept UI unchanged.
- Kept the source project `C:\Users\wordy\Desktop\OCR-Translator-3.9.6` untouched.
- Kept OCR scope limited to Custom AI OCR + tesserocr/Tesseract; no RapidOCR, Windows OCR, WinRT, EasyOCR, PaddleOCR, or other larger OCR engine was added.
- Kept Windows OCR removed. A read-only subagent audit found no Windows OCR/WinRT runtime-code, test, or config residue in the experiment copy; old mentions only remain in historical handoffs/backups/log material.
- `custom_ai.py` now includes `Source language: <lang>.` at the front of the OCR prompt only when the source language is explicitly set to a non-`auto` value.
- `auto` intentionally emits no language hint, preserving provider auto-detection behavior.
- `worker_threads.py` now routes Custom AI API OCR through `custom_source_lang` instead of deriving OCR source language from the currently selected translation provider's Gemini/OpenAI source-language setting.
- Verified the user's local tesserocr/Tesseract install is not PATH-only: Python package is under Python 3.12 site-packages, while Tesseract and tessdata are under `C:\Program Files\Tesseract-OCR`.
- Confirmed why earlier PATH-only checks miss this machine: `tesseract.exe` is not on PATH and `TESSDATA_PREFIX` is unset, but the Program Files install and registry entry exist.
- Official docs checked: tesserocr documents passing an explicit tessdata path to `PyTessBaseAPI`/`get_languages`; Tesseract docs explain that language data lives under `tessdata` and `TESSDATA_PREFIX`/`--tessdata-dir` affect lookup.

## Commands/tests run and their results

- Red tests before implementation:
  - `python -m unittest tests.test_custom_ai.CustomAIProviderTests.test_build_ocr_payload_includes_non_auto_source_language_hint -v`
  - Result: failed before implementation because OCR payload had no source-language hint.
  - `python -m unittest tests.test_custom_ai.CustomAIProviderTests.test_build_ocr_payload_omits_source_language_hint_for_auto -v`
  - Result: failed before implementation because the behavior was not implemented/tested yet.
  - `python -m unittest tests.test_latency_optimization.LatencyTranslationCacheTests.test_custom_ai_api_ocr_uses_custom_source_language -v`
  - Result: failed before implementation because Custom AI API OCR could be driven by translation-model source-language settings.
- Green focused tests after implementation:
  - Same three commands.
  - Result: `OK`.
- Fresh verification:
  - `python -m unittest tests.test_custom_ai -v`
  - Result: `OK`, 34 tests.
  - `python -m unittest tests.test_latency_optimization -v`
  - Result: `OK`, 38 tests.
  - `python -m compileall -q custom_ai.py worker_threads.py tests\test_custom_ai.py tests\test_latency_optimization.py`
  - Result: clean.
  - `python -m unittest discover -s tests -v`
  - Result: `OK`, 97 tests.
  - `python -m unittest discover -v`
  - Result: `OK`, 34 tests.
  - `$env:PYTHONDONTWRITEBYTECODE='1'; python -m unittest tests.test_latency_optimization.LatencyTesserocrTests -v`
  - Result: `OK`, 12 tests.
- Source isolation check:
  - `rg -n "test_build_ocr_payload_includes_non_auto_source_language_hint|test_build_ocr_payload_omits_source_language_hint_for_auto|test_custom_ai_api_ocr_uses_custom_source_language|Source language:" "C:\Users\wordy\Desktop\OCR-Translator-3.9.6" -g "!**/.codex/**" -g "!backups/**" -g "!dist/**" -g "!__pycache__/**"`
  - Result: no matches in the source project.
- Windows OCR / larger OCR residue check:
  - `rg -n "windows\.media\.ocr|Windows OCR|winrt|windows\.globalization|pytesseract|rapidocr|onnxruntime" . -g "!**/.codex/**" -g "!backups/**" -g "!dist/**" -g "!__pycache__/**"`
  - Result: only tests asserting removed OCR engines and old debug-log references; no current runtime code residue.
- Local tesserocr probe:
  - PowerShell here-string piped into `python -`
  - Result: Python `C:\Users\wordy\AppData\Local\Programs\Python\Python312\python.exe`; `tesserocr` package `C:\Users\wordy\AppData\Local\Programs\Python\Python312\Lib\site-packages\tesserocr\__init__.py`; `tesserocr.get_languages(None)` raises `TypeError`; explicit `C:\Program Files\Tesseract-OCR\tessdata` returns languages and includes `eng`; `TESSDATA_PREFIX=None`; `PATH_has_Tesseract=False`.
- Local install path probe:
  - Checked `C:\Program Files\Tesseract-OCR\tesseract.exe`, `C:\Program Files\Tesseract-OCR\tessdata`, and `C:\Users\wordy\AppData\Local\Programs\Python\Python312\Lib\site-packages\tesserocr`.
  - Result: all exist; `Get-Command tesseract.exe` returns no command because PATH does not include Tesseract.

## Subagent audit results

- Tesseract/tesserocr path audit:
  - Experiment copy now checks more than one location: explicit config path, `TESSDATA_PREFIX`, registry install dirs, Program Files, Program Files (x86), and LocalAppData style installs.
  - Local registry evidence: `HKLM\SOFTWARE\Tesseract-OCR InstallDir = C:\Program Files\Tesseract-OCR`.
  - Local site-packages evidence: `tesserocr` 2.10.0 is installed under Python 3.12.
  - Remaining packaging risk: if a future wheel or PyInstaller bundle places `tessdata` beside the `tesserocr` package, the current resolver does not explicitly scan that package-adjacent location unless config or `TESSDATA_PREFIX` points there.
  - PATH-only and `get_languages(None)` are both insufficient on this machine.
- Windows OCR / source isolation audit:
  - No current runtime Windows OCR/WinRT residue found in the experiment copy.
  - No current larger OCR engine production dependency/code residue found.
  - Source project has no matches for the exact new source-language prompt/tests added in this round.
  - Historical `.codex/handoffs`, `.codex/backups`, old `backups`, and old logs may still mention removed OCR engines; those are not runtime code.

## Current status

The experiment copy is verified green. Custom AI OCR now receives the Custom AI OCR source-language setting, and non-auto source languages are passed as a prompt hint to improve OCR behavior without adding another engine or changing the UI. Source project isolation was rechecked with exact-symbol search.

## Known issues or follow-up work

- PyInstaller/tesserocr packaging smoke test is still the next useful verification pass.
- Consider adding package-adjacent `tessdata` discovery for bundled/frozen builds if the smoke test shows PyInstaller does not preserve the Program Files/registry lookup path cleanly.
- Historical backups/logs still contain old Windows OCR/RapidOCR text; leave them alone unless an explicit cleanup request narrows that scope.

## Anything the next Codex session should read first

1. This handoff.
2. `.codex/handoffs/2026-06-23_17-58-44.md`
3. `.codex/handoffs/2026-06-23_17-42-48.md`
4. `custom_ai.py`
5. `worker_threads.py`
6. `ocr_utils.py`
7. `tests/test_custom_ai.py`
8. `tests/test_latency_optimization.py`
