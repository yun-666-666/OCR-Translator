# Task summary

Reviewed the latest application logs after the user's retest, then exposed the
missing local OCR tuning controls in the settings UI.

Latest log observations:

- The retest used `scan_interval = 100`, `stability_threshold = 2`, and
  `custom_ai_submit_interval_ms = 300`.
- The latest run stayed on Custom AI safe/non-stream requests; no stream events
  were found in the 20:50-20:59 log window.
- Translation total latency in that window: 145 samples, median 0.844s,
  p90 1.375s.
- PaddleOCR OCR in that window: 716 samples, median 0.141s, p90 0.266s.
- Translation queue events were still present (364), mostly when the selected
  source region included a large end-credit area (`1049x766`) rather than only
  the subtitle strip.

Implemented UI changes:

- Added a PaddleOCR-only `PaddleOCR minimum score` setting control wired to the
  existing `paddleocr_min_score_var`.
- Kept the confidence threshold Tesseract-only because the runtime path only
  applies `confidence_threshold` to Tesseract OCR.
- Renamed the confidence label in UI resources to make the Tesseract scope clear.

# Files changed or created

- `gui_builder.py`
  - Added float validation for 0.0-1.0 values.
  - Added `paddleocr_min_score_label` and `paddleocr_min_score_spinbox`.
  - Clamps PaddleOCR minimum score to 0.00-1.00 on focus out and saves settings.
- `handlers/ui_interaction_handler.py`
  - Shows PaddleOCR minimum score controls only when PaddleOCR is selected.
- `resources/gui_eng.csv`
  - Added `paddleocr_min_score_label`.
  - Clarified `confidence_threshold_label` as Tesseract-specific.
- `resources/gui_zh.csv`
  - Added `paddleocr_min_score_label`.
  - Clarified `confidence_threshold_label` as Tesseract-specific.
- `resources/gui_pol.csv`
  - Added `paddleocr_min_score_label`.
  - Clarified `confidence_threshold_label` as Tesseract-specific.
- `tests/test_custom_ai_startup.py`
  - Added coverage for the PaddleOCR minimum score control, visibility wiring,
    and localization labels.
- `.codex/handoffs/2026-07-06_21-08-32.md`
  - This handoff.

# Backup location

Backups were created before editing existing files:

- `.codex/backups/2026-07-06_21-04-03/`
  - `gui_builder.py`
  - `handlers/ui_interaction_handler.py`
  - `resources/gui_eng.csv`
  - `resources/gui_zh.csv`
  - `resources/gui_pol.csv`
  - `tests/test_custom_ai_startup.py`

# Tests/commands run and results

- `py -m unittest tests.test_custom_ai_startup.StartupOptimizationTests.test_local_ocr_threshold_controls_are_visible_and_localized`
  - Red before implementation: failed because `paddleocr_min_score_label` was not in `gui_builder.py`.
  - Passed after implementation.
- `py -m unittest tests.test_custom_ai_startup.StartupOptimizationTests.test_local_ocr_threshold_controls_are_visible_and_localized tests.test_custom_ai_startup.StartupOptimizationTests.test_new_custom_ai_controls_have_all_language_labels tests.test_paddle_ocr_backend.PaddleOCRWorkerRoutingTests`
  - Passed: 8 tests OK.
- `py -B -m py_compile gui_builder.py handlers\ui_interaction_handler.py tests\test_custom_ai_startup.py`
  - Passed with exit code 0.
- `git diff --check -- gui_builder.py handlers\ui_interaction_handler.py resources\gui_eng.csv resources\gui_zh.csv resources\gui_pol.csv tests\test_custom_ai_startup.py`
  - Passed with exit code 0.
- `py -m unittest tests.test_custom_ai_startup tests.test_paddle_ocr_backend.PaddleOCRWorkerRoutingTests.test_worker_builds_paddleocr_settings_from_app tests.test_paddle_ocr_backend.PaddleOCRWorkerRoutingTests.test_paddleocr_cache_key_includes_engine_settings`
  - Failed due an existing `custom_prompt.txt` default prompt mismatch in
    `test_shipped_custom_prompt_matches_runtime_default`.
  - This failure is unrelated to the OCR settings UI changes and was not fixed.

# Important decisions

- Did not show `confidence_threshold` for PaddleOCR because it is not used by
  the PaddleOCR runtime path.
- Added the PaddleOCR minimum score beside the existing OCR stability/threshold
  controls so the tuning controls are grouped together.
- Kept the change narrow: no runtime OCR filtering behavior was changed.

# Remaining issues or next steps

- The full `tests.test_custom_ai_startup` module still has an unrelated
  `custom_prompt.txt` mismatch that should be handled separately.
- If the selected source region includes large credit screens instead of only
  subtitle lines, PaddleOCR will still feed larger text blocks into translation.
  Region selection or an additional large-area filtering rule may be worth
  considering separately.
