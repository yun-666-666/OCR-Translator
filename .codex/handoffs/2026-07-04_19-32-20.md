# Task summary

Completed big optimization #19: immutable latency-mode request strategy.

Normalized latency mode is now part of the in-flight identity and is captured
once by the worker. That same value controls stream callback creation and is
passed through the handler to provider execution. Switching safe/stream/race
while a request is queued can no longer mix two execution strategies.

# Files changed or created

- `handlers/translation_handler.py`
- `worker_threads.py`
- `tests/test_custom_ai.py`
- `tests/test_latency_optimization.py`
- design and plan documents
- this handoff

# Backup location

- `.codex/backups/2026-07-04_19-31-05/`

# Verification

- TDD RED proved identical safe/stream in-flight keys and no override support.
- Focused tests passed after implementation.
- `tests.test_custom_ai`: 146 passed.
- `tests.test_latency_optimization`: 68 passed.
- `discover -s tests`: 254 passed.
- root discovery: 35 passed.
- compileall and diff check passed.

# Decisions

- Direct callers without an override still read current UI mode.
- Worker-owned requests use one normalized snapshot end to end.
- Updated strict test doubles to the new interface contract.
- `.codex/release-artifacts/` remains untouched.

# Next steps

- Continue with the next independent large optimization after push.
