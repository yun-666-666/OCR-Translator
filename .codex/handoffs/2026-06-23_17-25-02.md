# API OCR cache key hardening handoff

## Task summary

Continued the isolated optimization pass in the experiment copy only. This round tightened the API OCR frame-cache key so cached AI OCR results do not cross Custom AI OCR profiles or `keep_linebreaks` mode changes.

## Files changed

- `worker_threads.py`
- `tests/test_latency_optimization.py`

## Backup location

- `.codex/backups/2026-06-23_17-19-14/`

Backed up before edits:

- `worker_threads.py`
- `tests/test_latency_optimization.py`

## Important decisions made

- Kept UI unchanged.
- Kept the source project `C:\Users\wordy\Desktop\OCR-Translator-3.9.6` untouched.
- Kept the OCR scope limited to AI OCR + tesserocr, with no larger OCR engine added.
- Added a Custom AI OCR cache model key that includes active OCR profile id, base URL, and model.
- Added an API OCR mode key that includes `keep_linebreaks` when that setting is available.
- Kept the fallback cache key unchanged for non-Custom AI or test objects without a profile manager, so existing behavior remains stable outside the stricter real-app path.

## Commands/tests run and their results

- Red tests:
  - `python -m unittest tests.test_latency_optimization.LatencyTranslationCacheTests.test_api_ocr_cache_does_not_cross_custom_ai_ocr_profiles -v`
  - Result: failed before implementation because the old generic `custom_ai` cache key was reused without reading the active OCR profile.
  - `python -m unittest tests.test_latency_optimization.LatencyTranslationCacheTests.test_api_ocr_cache_does_not_cross_keep_linebreaks_mode -v`
  - Result: failed before implementation because a generic `api` cache entry was reused when `keep_linebreaks=True`.
- Focused green tests:
  - `python -m unittest tests.test_latency_optimization.LatencyTranslationCacheTests.test_api_ocr_cache_does_not_cross_custom_ai_ocr_profiles tests.test_latency_optimization.LatencyTranslationCacheTests.test_api_ocr_cache_does_not_cross_keep_linebreaks_mode tests.test_latency_optimization.LatencyTranslationCacheTests.test_api_ocr_cache_hit_reuses_cached_text_without_webp_or_submit -v`
  - Result: `OK`, 3 tests.
- OCR/runtime regression subset:
  - `python -m unittest tests.test_latency_optimization -v`
  - Result: `OK`, 28 tests.
- Full test suite:
  - `python -m unittest discover -s tests -v`
  - Result: `OK`, 85 tests.
- Root discovery:
  - `python -m unittest discover -v`
  - Result: `OK`, 34 tests.
- Syntax check:
  - `python -m compileall -q worker_threads.py tests\\test_latency_optimization.py`
  - Result: clean.
- Real local tesserocr probe:
  - `python -c "import ocr_utils; print(ocr_utils.get_tesseract_languages(None)[0]); print('eng' in ocr_utils.get_tesseract_languages(None)[1])"`
  - Result: resolved `C:\Program Files\Tesseract-OCR\tessdata` and printed `True`.
- Source isolation check:
  - `rg -n "test_api_ocr_cache_does_not_cross_custom_ai_ocr_profiles|test_api_ocr_cache_does_not_cross_keep_linebreaks_mode|_get_api_ocr_cache_model_key|_get_api_ocr_cache_mode_key" "C:\\Users\\wordy\\Desktop\\OCR-Translator-3.9.6" -g "!**/.codex/**" -g "!backups/**" -g "!dist/**"`
  - Result: no matches in the source project.

## Current status

The experiment copy is verified green. API OCR cache hits are now stricter: cached OCR text is isolated by frame, provider/profile, source language, API OCR mode, region size, and region origin.

## Known issues or follow-up work

- PyInstaller packaging for tesserocr DLLs/tessdata discovery has not been re-tested in this pass.
- A future pass could run a packaging smoke check if the user wants the experiment copy prepared for executable packaging.

## Anything the next Codex session should read first

1. This handoff.
2. `worker_threads.py`
3. `tests/test_latency_optimization.py`
4. `custom_ai.py`
