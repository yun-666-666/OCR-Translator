# Profile refresh uses guarded UI scheduler

## Candidate selection

Reviewed remaining live-path Tk / `root.after` sites after Round 2 (`8ea172e`).

Candidates considered:

1. `refresh_translation_after_profile_change` still used raw `app.root.after(0, ...)` without shutdown / `winfo_exists` / TclError guards. **Selected.**
2. Pending translation flush delayed `root.after`. Deferred: needs delay support on `_schedule_ui_callback`.
3. OCR stability flush delayed `root.after`. Deferred: same delay-support concern.
4. Instant-cache Tk var read inside `start_async_translation`. Deferred: entrypoints already UI-scheduled.
5. Translation-sequence counter TOCTOU. Deferred: medium risk, broader locking design.
6. Compatibility `run_api_ocr` live Tk fallbacks without snapshot. Deferred: production path always supplies snapshot.
7. Worker latency-mode fallback Tk reads when snapshot missing. Deferred: production path freezes latency mode in request snapshot before worker.

Selection criteria met for item 1:

- High confidence static proof: delay is already 0; raw `root.after` can still fire after close / destroyed root.
- Low risk: swap to `_schedule_ui_callback`, restore generation and leave pending request intact on schedule failure (fail-closed).
- <=4 files: only `worker_translation.py` + tests + handoff.
- Deterministic tests without network.
- User-visible stability: avoids post-shutdown profile-refresh scheduling races while preserving pending work.

## Task summary

`refresh_translation_after_profile_change` now schedules `_apply_translation_profile_refresh` through `_schedule_ui_callback`. If scheduling fails, generation is restored, pending request is preserved, and the function returns `False` instead of raising or invalidating pending work.

## Files changed or created

- `worker_translation.py`
- `tests/test_latency_optimization.py`
- `.codex/handoffs/2026-07-20_22-39-52.md`

## Backup and rollback

- Backup: `.codex/backups/2026-07-20_22-39-52/`
- Branch: `codex/grok-auto-optimization-20260720`
- Rollback branch: `codex/backup-before-grok-auto-optimization-20260720-222943`

## Validation

- `python -m unittest tests.test_latency_optimization.LiveCustomAIModelSwitchTests tests.test_latency_optimization.LiveCustomAIProfileSwitchTests -v`: 15 passed.
- `python -m unittest tests.test_latency_optimization -q`: 179 passed.
- `python -m unittest discover -s tests -p 'test_*.py' -q`: 724 passed.
- `python -m py_compile worker_translation.py tests/test_latency_optimization.py`: passed.
- `git diff --check -- worker_translation.py tests/test_latency_optimization.py`: passed.

## Re-review of this diff

- Happy-path schedule still works (generation bump + pending invalidation only after successful schedule).
- Schedule failure is fail-closed: pending request, flush flags, and generation restored.
- Closing / destroyed / stopped app guards covered by new test.
- No cache/snapshot/keep_linebreaks/UI-routing baseline regressions introduced.
- Scope stayed inside profile-refresh scheduling only.

## Remaining issues or next steps

- Delayed raw `root.after` sites (pending flush, OCR stability flush) still need a delay-aware guarded scheduler.
- Translation-sequence locking remains a broader follow-up.
- Loop max of 3 rounds reached; stop here.
