# Task summary

Completed big optimization #22: RFC-safe race endpoint URL canonicalization.

After wire-path expansion, race identity now lowercases scheme/hostname and
removes HTTP port 80 or HTTPS port 443. It preserves user info, non-default
ports, path case, query, and fragment, so only network-equivalent endpoints are
deduplicated.

# Files changed or created

- `handlers/translation_handler.py`
- `tests/test_custom_ai.py`
- design and plan documents
- this handoff

# Backup location

- `.codex/backups/2026-07-04_19-36-48/`

# Verification

- TDD RED reproduced HTTPS/443 and HTTP/80 duplicates.
- Focused canonicalization and path-semantics tests passed.
- `tests.test_custom_ai`: 151 passed.
- `tests.test_latency_optimization`: 68 passed.
- `discover -s tests`: 259 passed.
- root discovery: 35 passed.
- compileall and diff check passed.

# Decisions

- Canonicalized only URL components with safe equivalence rules.
- Preserved case-sensitive paths and non-default ports.
- Malformed URLs retain the original fallback identity.
- `.codex/release-artifacts/` remains untouched.

# Next steps

- Continue with the next independent large optimization after push.
