# API OCR stale worker gate handoff

## Task summary

Continued the isolated optimization pass in the experiment copy only. This round added a lightweight stale-request gate to the async API OCR worker so an older queued OCR batch does not call the AI OCR provider if a newer batch has already started before the worker begins.

## Files changed

- `worker_threads.py`
- `tests/test_latency_optimization.py`

## Backup location

- `.codex/backups/2026-06-23_16-52-01/`

Backed up before edits:

- `worker_threads.py`
- `tests/test_latency_optimization.py`

## Important decisions made

- Kept UI unchanged.
- Kept the source project `C:\Users\wordy\Desktop\OCR-Translator-3.9.6` untouched.
- Kept the OCR scope limited to AI OCR + tesserocr, with no larger OCR engine added.
- Added the stale gate inside `process_api_ocr_async()` so queued-but-obsolete work is skipped before making a provider/network call.
- Kept the existing `finally` cleanup path intact so skipped stale batches still leave `active_ocr_calls`.

## Commands/tests run and their results

- Red test:
  - `python -m unittest tests.test_latency_optimization.LatencyTranslationCacheTests.test_stale_api_ocr_request_skips_provider_call -v`
  - Result: failed as expected before the code change because `perform_ocr()` was still called for an older batch.
- Focused green test:
  - `python -m unittest tests.test_latency_optimization.LatencyTranslationCacheTests.test_stale_api_ocr_request_skips_provider_call -v`
  - Result: `OK`
- Latency regression subset:
  - `python -m unittest tests.test_latency_optimization -v`
  - Result: `OK`, 22 tests.
- Full test suite:
  - `python -m unittest discover -s tests -v`
  - Result: `OK`, 79 tests.
- Root discovery:
  - `python -m unittest discover -v`
  - Result: `OK`, 34 tests.
- Syntax check:
  - `python -m compileall -q worker_threads.py tests\\test_latency_optimization.py`
  - Result: clean.
- Source isolation check:
  - `rg -n "stale \\(latest started|skipping provider call|test_stale_api_ocr_request_skips_provider_call" "C:\\Users\\wordy\\Desktop\\OCR-Translator-3.9.6" -g "!**/.codex/**" -g "!backups/**" -g "!dist/**"`
  - Result: no matches in the source project.

## Current status

The experiment copy is verified green. API OCR now avoids one more stale-work path: an older queued batch exits before calling the provider if a newer OCR batch has already started.

## Known issues or follow-up work

- A next low-risk pass could validate that the resolved tesserocr tessdata directory actually contains the selected language before starting local OCR, because directory existence alone does not prove the traineddata file is available.
- PyInstaller packaging for tesserocr DLLs/tessdata discovery has not been re-tested in this pass.

## Anything the next Codex session should read first

1. This handoff.
2. `worker_threads.py`
3. `tests/test_latency_optimization.py`
4. `ocr_utils.py`
