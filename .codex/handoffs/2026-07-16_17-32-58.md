# OCR Translator v3.10.4 Release Preparation Handoff

## Task summary

- Prepared the complete current local branch for one-way publication to GitHub
  as `v3.10.4`.
- Kept the local branch as the sole source of truth. No `git pull`, merge,
  rebase, remote checkout, or remote-content overwrite was performed.
- Included all verified local work since `v3.10.3`: log-driven translation and
  OCR pacing, subtitle layout/centering/bold controls, unified automatic AI/OCR
  optimization, MSS-only capture, compact settings layout, and target-border
  opacity synchronization.
- Updated package metadata and both changelog copies for the patch release.

## Files changed or created for this publication

- `setup.py`
- `CHANGELOG.md`
- `docs/CHANGELOG.md`
- `pyside_overlay.py`
- `tests/test_overlay_startup.py`
- `.codex/handoffs/2026-07-16_17-27-15.md`
- `.codex/handoffs/2026-07-16_17-32-58.md`

The new release notes and tracked-source ZIP are generated under the ignored
`.codex/release-artifacts/` directory after the release commit is created.

## Backup locations

- `.codex/backups/2026-07-16_17-26-04/`
  - `pyside_overlay.py`
  - `tests/test_overlay_startup.py`
- `.codex/backups/2026-07-16_17-31-08/`
  - `setup.py`
  - `CHANGELOG.md`
  - `docs/CHANGELOG.md`

Every backup was SHA-256 checked against its source before editing.

## Tests and commands run

- `py setup.py --version`
  - Reported `3.10.4`.
- `py -m unittest discover -s tests -q`
  - 581 passed.
- `py -m unittest -q`
  - 35 passed.
- `py -B -m py_compile` over all changed Python files since `v3.10.3` plus
  current uncommitted Python changes
  - Exit code 0.
- `git diff --check`
  - Clean.
- `git diff --cached --check`
  - Clean before staging.
- Tracked-path scan
  - No live config, `.env`, runtime log, backup directory, `__pycache__`, or
    release ZIP is tracked.
- Boundary-aware credential-pattern scan of tracked non-test/non-doc runtime
  files
  - No credential-like value found.
- `gh auth status`
  - Authenticated to `github.com` as repository owner `yun-666-666` with
    `repo` access.
- `gh release view v3.10.4`
  - Confirmed the release does not exist.
- `git ls-remote --tags origin refs/tags/v3.10.4`
  - Confirmed the remote tag is free.

## Important decisions

- Publish branch `codex/ai-auto-settings-20260716` directly from its current
  local state.
- Use patch version `v3.10.4` because the changes are backward-compatible
  feature, performance, usability, and bug-fix improvements.
- Do not merge into or update local `main`; do not import any remote branch
  content.
- Build `OCR-Translator-v3.10.4-source.zip` from the final tracked release
  commit with `git archive`.
- Create a real GitHub Release marked Latest and attach the source ZIP with a
  published SHA-256 digest.
- Do not publish a binary executable because no executable was rebuilt or
  verified for this task.
- Preserve and exclude the pre-existing untracked
  `.codex/release-artifacts/release-notes-v3.10.3-2026-07-15.md`.

## Remaining publication steps

- Stage only the intended local release files and create the release commit.
- Push the local branch to `origin`.
- Generate the tracked source ZIP and SHA-256 digest from the release commit.
- Create GitHub Release `v3.10.4` targeting that exact commit.
- Verify the remote branch, release tag, release page, asset name, size, and
  digest.
