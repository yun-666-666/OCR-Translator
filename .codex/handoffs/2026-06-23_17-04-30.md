# Tesserocr language availability cache handoff

## Task summary

Continued the isolated optimization pass in the experiment copy only. This round added cached tesserocr language discovery and a fail-fast language availability check before creating `PyTessBaseAPI`, so local OCR no longer relies only on tessdata directory existence.

## Files changed

- `ocr_utils.py`
- `app_logic.py`
- `tests/test_latency_optimization.py`
- `tests/test_custom_ai_startup.py`

## Backup location

- `.codex/backups/2026-06-23_16-57-25/`

Backed up before edits:

- `ocr_utils.py`
- `tests/test_latency_optimization.py`
- `tests/test_custom_ai_startup.py`

## Important decisions made

- Kept UI unchanged.
- Kept the source project `C:\Users\wordy\Desktop\OCR-Translator-3.9.6` untouched.
- Kept the OCR scope limited to AI OCR + tesserocr, with no larger OCR engine added.
- Used `tesserocr.get_languages()` behind a small cached helper so language availability is checked once per resolved tessdata directory instead of re-scanning on every local OCR call.
- Added a fail-fast check before `PyTessBaseAPI` creation. If the requested language is missing from tessdata, the app now raises a clearer `TesseractOcrUnavailableError` before attempting API initialization.
- Wired the new language-list cache into the existing Tesseract runtime cache clear path.

## Commands/tests run and their results

- Initial red/focused tests:
  - `python -m unittest tests.test_latency_optimization.LatencyTesserocrTests.test_tesseract_language_lookup_is_cached_until_cleared tests.test_latency_optimization.LatencyTesserocrTests.test_tesserocr_engine_fails_fast_when_requested_language_is_missing -v`
  - Result: failed before implementation because `clear_tesseract_languages_cache()` did not exist, then the fast-fail assertion was tightened to prove `PyTessBaseAPI` is not created for missing languages.
- Focused green tests:
  - `python -m unittest tests.test_latency_optimization.LatencyTesserocrTests.test_tesseract_language_lookup_is_cached_until_cleared tests.test_latency_optimization.LatencyTesserocrTests.test_tesserocr_engine_fails_fast_when_requested_language_is_missing -v`
  - Result: `OK`, 2 tests.
  - `python -m unittest tests.test_custom_ai_startup.StartupOptimizationTests.test_tesseract_runtime_cache_is_cleared_when_ocr_parameters_change tests.test_custom_ai_startup.StartupOptimizationTests.test_tesseract_runtime_cache_is_cleared_when_ocr_model_changes tests.test_custom_ai_startup.StartupOptimizationTests.test_tesseract_runtime_cache_clear_is_wired_into_start_and_stop_paths -v`
  - Result: `OK`, 3 tests.
- Real local tesserocr probe:
  - `python -c "import ocr_utils; print(ocr_utils.get_tesseract_languages(None)[0]); print('eng' in ocr_utils.get_tesseract_languages(None)[1])"`
  - Result: resolved `C:\Program Files\Tesseract-OCR\tessdata` and printed `True` for `eng`.
- Focused regression:
  - `python -m unittest tests.test_latency_optimization -v`
  - Result: `OK`, 24 tests.
  - `python -m unittest tests.test_custom_ai_startup -v`
  - Result: `OK`, 13 tests.
- Full test suite:
  - `python -m unittest discover -s tests -v`
  - Result: `OK`, 81 tests.
- Root discovery:
  - `python -m unittest discover -v`
  - Result: `OK`, 34 tests.
- Syntax check:
  - `python -m compileall -q ocr_utils.py app_logic.py tests\\test_latency_optimization.py tests\\test_custom_ai_startup.py`
  - Result: clean.
- Source isolation check:
  - `rg -n "get_tesseract_languages|clear_tesseract_languages_cache|test_tesseract_language_lookup_is_cached_until_cleared|test_tesserocr_engine_fails_fast_when_requested_language_is_missing" "C:\\Users\\wordy\\Desktop\\OCR-Translator-3.9.6" -g "!**/.codex/**" -g "!backups/**" -g "!dist/**"`
  - Result: no matches in the source project.

## Current status

The experiment copy is verified green. Local OCR now verifies that the selected Tesseract language is available in the resolved tessdata directory before initializing the reusable tesserocr API, and that language discovery is cached and cleared with the rest of the Tesseract runtime state.

## Known issues or follow-up work

- PyInstaller packaging for tesserocr DLLs/tessdata discovery has not been re-tested in this pass.
- A future pass could add a package/build smoke check if the user wants the experiment copy prepared for executable packaging.

## Anything the next Codex session should read first

1. This handoff.
2. `ocr_utils.py`
3. `app_logic.py`
4. `tests/test_latency_optimization.py`
5. `tests/test_custom_ai_startup.py`
