# Task summary

Verified and closed the four review follow-up items from the project status
audit.

The shipped example config now matches the runtime Custom AI latency default:
`custom_ai_latency_mode = safe`. English, Chinese, and Polish UI resources now
include labels for the adaptive latency option, structured output profile
control, Custom AI OCR image payload mode/quality/detail controls, and the
Polish submit interval label.

Added regression coverage so the example latency default stays aligned with
`DEFAULT_CONFIG_SETTINGS`, and so the new Custom AI UI controls require
non-empty labels in all three shipped GUI CSV files.

Ran a real Custom AI relay smoke test using the user-provided relay details in
temporary environment variables only. The key was not written to repository
files. The relay returned 18 models, `codex-auto-review` passed the profile
`Reply OK` check, and a structured translation request returned
`Hello, world!` -> `\u4f60\u597d\uff0c\u4e16\u754c\uff01` with usage metadata.

# Files changed or created

- `ocr_translator_config.example.ini`
- `resources/gui_eng.csv`
- `resources/gui_zh.csv`
- `resources/gui_pol.csv`
- `tests/test_custom_ai_startup.py`
- `.codex/handoffs/2026-07-05_17-09-25.md`

# Backup location

- `.codex/backups/2026-07-05_17-04-15/`

Backup contains originals for changed existing files:

- `tests/test_custom_ai_startup.py`
- `ocr_translator_config.example.ini`
- `resources/gui_eng.csv`
- `resources/gui_zh.csv`
- `resources/gui_pol.csv`

# Tests/commands run and results

- TDD RED:
  `python -m unittest tests.test_custom_ai_startup.StartupOptimizationTests.test_example_config_matches_custom_ai_latency_default tests.test_custom_ai_startup.StartupOptimizationTests.test_new_custom_ai_controls_have_all_language_labels -q`
  - Failed as expected:
    - example config used `custom_ai_latency_mode = none`;
    - GUI CSV files missed new Custom AI label keys.
- TDD GREEN:
  `python -m unittest tests.test_custom_ai_startup.StartupOptimizationTests.test_example_config_matches_custom_ai_latency_default tests.test_custom_ai_startup.StartupOptimizationTests.test_new_custom_ai_controls_have_all_language_labels -q`
  - 2 passed.
- Real relay smoke test with temporary environment variables:
  - `/models` probe: OK, 18 models.
  - Selected model: `codex-auto-review`.
  - Profile test response: `OK`.
  - Structured translation contract: `json_schema`.
  - Translation result: `\u4f60\u597d\uff0c\u4e16\u754c\uff01`.
  - Usage keys included prompt/input/output/cached token aliases and
    `cached_input_ratio`.
- `python -m unittest discover -s tests`
  - 339 passed.
- `python -m unittest discover`
  - 35 passed.
- `python -m compileall tests\test_custom_ai_startup.py ocr_translator_config.example.ini resources\gui_eng.csv resources\gui_zh.csv resources\gui_pol.csv`
  - Passed; compiled the Python test file.
- `git diff --check`
  - Passed after normalizing `resources/gui_pol.csv` line endings.
- Workspace secret scan for the provided API key:
  - No matches found in workspace files.

# Important decisions

- Kept the fix scoped to shipped config/resources and regression tests; no
  runtime code changes were needed.
- Preserved `.codex/release-artifacts/` exactly as requested. It remains a local
  untracked/ignored release artifact area.
- Used the real relay key only through process environment variables for smoke
  testing and disabled debug logging during the API calls.
- Did not attempt vision/OCR API smoke testing because no vision-capable Custom
  AI profile/model was specified for the relay. The real verification covered
  Custom AI model listing, profile testing, structured text translation, and
  usage extraction.

# Remaining issues or next steps

- `.codex/release-artifacts/` remains present and untracked by choice.
- The current branch is still separate from `origin/main`; integration with
  main requires a later merge/rebase decision.
