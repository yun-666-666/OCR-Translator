# Clone-safe startup residue test handoff

## Task summary

Updated both duplicate static RapidOCR residue tests to scan the tracked
`ocr_translator_config.example.ini` artifact instead of the intentionally
ignored runtime `ocr_translator_config.ini`. No production behavior changed.

The clean-worktree `FileNotFoundError` results reproduced before this edit were
used as the authoritative TDD RED evidence.

## Files changed or created

- `tests/test_custom_ai_startup.py`
- `test_custom_ai_startup.py`
- `.codex/handoffs/2026-07-13_03-15-05.md`

## Backup location

`.codex/backups/2026-07-13_03-14-21/`

Both test files were copied before editing with their workspace-relative paths
preserved. SHA-256 comparisons between each source and backup returned
`MATCH=True`.

## Tests and commands run

- `py -m unittest tests.test_custom_ai_startup.StartupOptimizationTests.test_rapidocr_support_is_removed_from_startup_files -v` -> 1 test passed.
- `py -m unittest test_custom_ai_startup.StartupOptimizationTests.test_rapidocr_support_is_removed_from_startup_files -v` -> 1 test passed.
- `py -m unittest discover -s tests` -> 504 tests passed.
- `py -m unittest test_custom_ai test_custom_ai_startup -q` -> 35 tests passed.
- `git diff --check` -> passed.
- Exact diff audit -> only the two requested config-path substitutions; backup files are ignored by `.gitignore`.

## Important decisions

- Used the tracked example config because these methods perform static residue
  scans and must work in a clean clone.
- Kept the fix test-only and identical in both duplicate test modules.
- Did not read or modify `Dango-Translator-Ver6.3.1/`.

## Remaining issues or next steps

- No known concerns for this prerequisite fix.
- Resume the planned project-wide optimization work from the latest security
  increment plan.
