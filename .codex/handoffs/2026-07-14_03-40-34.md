# Final Audit Security Follow-up Handoff

## Task summary

The independent final audit found a Custom AI streaming fallback diagnostic that rendered the raw upstream exception. This follow-up replaces that rendering with the existing bounded sanitizer, adds a regression test that proves token and response-body markers are not logged, and applies the same sanitizer at the remaining Custom AI error boundaries discovered during the audit.

## Files changed or created

- Modified `custom_ai_transport.py`, `custom_ai_profiles.py`, `gui_builder.py`, `gui_profile_controls.py`, `worker_ocr.py`, and `worker_translation.py`.
- Modified `handlers/translation_requests.py`, `handlers/translation_handler.py`, `handlers/translation_results.py`, and `handlers/translation_context.py`.
- Modified `tests/test_custom_ai.py`.
- Created `.codex/handoffs/2026-07-14_03-40-34.md`.

## Backup location

- `.codex/backups/2026-07-14_03-36-47/`: SHA-256-verified current copies of all eleven existing edit targets before this follow-up.

## Tests and commands run

- RED: `py -m unittest tests.test_custom_ai.CustomAIProviderTests.test_stream_transient_fallback_log_sanitizes_error -q` failed before the fix because the unique token marker appeared in the debug log.
- GREEN: the same focused test passed after the fix.
- `py -m unittest tests.test_custom_ai -q`: 311 passed.
- `py -m unittest tests.test_latency_optimization tests.test_runtime_logging -q`: 209 passed.
- `py scripts/run_offline_tests.py`: 646 discovery tests plus 35 legacy Custom AI tests, 681 total passed.
- Custom AI exception-rendering audit and `git diff --check`: passed.

## Important decisions

- All changed Custom AI diagnostic and related UI error boundaries now use `sanitize_error_text` or the provider-aware `_sanitize_error` helper. Request routing, deadlines, fallback behavior, and UI flow are unchanged.
- The stream fallback uses the profile API key as a known secret before writing the diagnostic; the regression test also covers separately named token and response-body fields.
- No provider request, package installation, build, or environment mutation was performed.

## Remaining issues or next steps

- Repeat the full final completion audit (offline suite, compilation, diff/scope/secret checks, clean status), commit this security follow-up, then push the verified branch to GitHub.
