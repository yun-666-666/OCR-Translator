# Paid Custom AI validation and publish handoff

## Task summary

Performed user-authorized paid validation against the application's saved
Custom AI configuration, then prepared the remaining source work for a
secret-safe GitHub commit and push.

The desktop-control launch request timed out and did not produce a targetable
application window, so no alternate UI automation was used.  The real test
instead instantiated the application's existing `CustomAIProfileManager` and
`TranslationHandler` with the saved credentials and a temporary cache.

## Real-provider evidence

- Current active profile `e2` first failed after 11.625 seconds; the handler
  then tried `xai` once and received a usable translation in 1.906 seconds.
  This proves real sequential profile failover with the stored configuration.
- A second real request selected saved profile `234` first in memory without
  persisting any profile change.  `234` returned a usable translation in 3.547
  seconds on its first and only call.  This proves the forced-SSE response is
  now parsed successfully instead of becoming an error loop.
- Both tests used the shortest source text (`OK`), a temporary translation
  cache, and emitted no API keys or response bodies.

## Rust removal

The user explicitly requested that `rust/` not be committed and be deleted.

- Verified the absolute delete target was inside this workspace.
- Backed up the nine source and lock files under `rust/` to the backup location
  below.
- Deleted `rust/` including its 394 generated `target/` build files (about
  90 MB); generated output was not copied to the backup.
- Verified no `rust/` path remains in the future staging candidates.

## Files changed or created

- `.codex/handoffs/2026-07-12_13-10-56.md`
- The existing Python/module-split, Custom AI failover, worker, tests, docs,
  and previous handoff files already present in the working tree are prepared
  for commit.
- No runtime profile, credential, API-key, SQLite cache, or debug-log file is
  included in the commit candidate set.

## Backup locations

- `.codex/backups/2026-07-12_13-04-55/` — runtime logs, active configuration,
  profile store, translation cache, and pre-edit `.gitignore`.
- `.codex/backups/2026-07-12_13-10-26/` — deleted `rust/` source and lock files.

## Verification

- `py -m unittest discover -s tests` -> 504 passed after Rust removal.
- Candidate secret scan -> passed; no API-key, bearer-token, private-key, cloud
  credential, or runtime/config file was found in the commit candidate set.
- Candidate check -> no `rust/` path remains.

## Important decisions

- Rust source and its generated output are both deleted at the user's request;
  neither will be committed.
- The regular application executable was not used for the paid run because the
  supported Windows-control launch path timed out.  The validation still used
  the actual app profile manager, actual credentials, actual translation
  handler, and real provider network calls.
- Before committing, stage all remaining tracked and untracked source/docs/
  handoff files, rerun a staged-file secret scan and `git diff --cached --check`,
  then commit and push the current branch.
