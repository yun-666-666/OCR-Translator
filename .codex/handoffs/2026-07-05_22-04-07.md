# Task summary

按用户要求回退上一轮 `custom_ai.py` 低风险拆分。已恢复 `custom_ai.py` 到拆分前备份版本，并删除上一轮新增的 `custom_ai_constants.py`。

# Files changed or created

- `custom_ai.py`
  - 已从 `.codex/backups/2026-07-05_21-43-22/custom_ai.py` 恢复。
  - 恢复后不再依赖 `custom_ai_constants.py`。
- `custom_ai_constants.py`
  - 已删除。
- `.codex/handoffs/2026-07-05_22-04-07.md`
  - 新增本回退交接文档。

# Backup location

本次回退前备份：

- `.codex/backups/2026-07-05_22-02-30/`

已备份回退前现有文件：

- `custom_ai.py`
- `custom_ai_constants.py`

回退来源：

- `.codex/backups/2026-07-05_21-43-22/custom_ai.py`

# Tests/commands run and results

- `python -B -m py_compile custom_ai.py`
  - Passed.
- `python -B -c "from custom_ai import CustomAIProvider, CustomAIProfileManager, normalize_custom_ai_latency_mode; print('PUBLIC_IMPORTS_OK', normalize_custom_ai_latency_mode('adaptive'))"`
  - Passed: `PUBLIC_IMPORTS_OK adaptive`.
- `python -B -m unittest tests.test_custom_ai`
  - Passed: 209 tests ran in 0.886s.
- `python -B -m unittest tests.test_custom_ai_startup`
  - Passed: 20 tests ran in 0.216s.
- `python -B -m unittest tests.test_latency_optimization`
  - Passed: 103 tests ran in 0.966s.
- `python -B -m unittest discover -s tests -p "test_*.py"`
  - Passed: 365 tests ran in 2.632s.
- `git diff -- custom_ai.py custom_ai_constants.py`
  - No diff output after rollback.
- `git diff --check -- custom_ai.py .codex/handoffs/2026-07-05_21-47-58.md`
  - Passed.
- Workspace secret-pattern scan by filename only:
  - Passed: no `sk-...` pattern matches found in workspace files outside ignored generated/dependency folders.

# Real API verification

Used the relay credential only in memory from the provided attachment file. The credential was not written to code, tests, config, logs, this handoff, or any other persistent file.

- Result: success.
- base_url: `https://shayulajiao.xyz`
- model: `codex-auto-review`
- returned text prefix: `你好`
- elapsed_seconds: `3.469`

# Important decisions

- Did not use destructive git commands.
- Did not touch unrelated existing workspace changes in `app_logic.py`, `config_manager.py`, `ocr_utils.py`, `worker_threads.py`, prior handoffs, release artifacts, or `tests/test_adaptive_block_size.py`.
- Left the previous split handoff `.codex/handoffs/2026-07-05_21-47-58.md` in place as historical record; this new handoff records the rollback.

# Remaining issues or next steps

- No rollback-specific test failures remain.
- If this rollback itself needs to be undone, restore `custom_ai.py` and `custom_ai_constants.py` from `.codex/backups/2026-07-05_22-02-30/`.
