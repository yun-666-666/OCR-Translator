# Responses API and UI language fix handoff

## Task summary

Extended the experiment copy with the same fix set:

- Custom AI profiles can now declare `wire_api = responses`, so the profile
  named `测试2` works with the ebandflow relay.
- Legacy GUI language display names normalize to `中文` instead of being saved
  back as mojibake.

## Files changed

- `custom_ai.py`
- `language_ui.py`
- `app_logic.py`
- `handlers/ui_interaction_handler.py`
- `tests/test_custom_ai.py`
- `custom_ai_profiles.json`
- `ocr_translator_config.ini`

## Backup location

- `.codex/backups/2026-06-23_19-25-55/`

Backed up before edits:

- `custom_ai.py`
- `language_ui.py`
- `app_logic.py`
- `handlers/ui_interaction_handler.py`
- `tests/test_custom_ai.py`
- `custom_ai_profiles.json`
- `ocr_translator_config.ini`

## Important decisions made

- Kept the existing tesserocr/Tesseract optimization work intact.
- Added a dedicated Responses path instead of trying to coerce those profiles
  through Chat Completions.
- Kept OpenRouter and chat-completions behavior unchanged.
- Preserved the experiment copy's OCR source-language hint behavior.
- Updated the experiment profile file so its active `测试2` profile is a
  Responses profile and stays enabled.

## Commands/tests run and their results

- Focused regression tests:
  - `python -m unittest tests.test_custom_ai.CustomAIProfileManagerTests.test_profile_manager_persists_responses_wire_api ... -v`
  - Result: `OK`
- Full Custom AI suite:
  - `python -m unittest tests.test_custom_ai -v`
  - Result: `Ran 41 tests`, `OK`
- Full test discovery:
  - `python -m unittest discover -s tests -v`
  - Result: `Ran 107 tests`, `OK`
- Root discovery:
  - `python -m unittest discover -v`
  - Result: `Ran 34 tests`, `OK`

## Current status

Experiment workspace is green and now supports the same usable Responses
profile path as the main workspace.

## Known issues or follow-up work

- No packaging/spec files were changed.
- I did not alter any unrelated OCR/tesserocr improvements.

## Anything the next Codex session should read first

1. This handoff.
2. `custom_ai.py`
3. `tests/test_custom_ai.py`
4. `language_ui.py`
5. `handlers/ui_interaction_handler.py`
